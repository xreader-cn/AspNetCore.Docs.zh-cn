---
title: 从 ASP.NET Core 2.1 迁移到 2.2
author: scottaddie
description: 本文概述了将 ASP.NET Core 2.1 项目迁移到 ASP.NET Core 2.2 的先决条件和最常见步骤。
ms.author: scaddie
ms.custom: mvc
ms.date: 05/28/2019
uid: migration/21-to-22
ms.openlocfilehash: ac87d97a259c230c01d236e84d6e42b28adad7e0
ms.sourcegitcommit: 2cb857f0de774df421e35289662ba92cfe56ffd1
ms.translationtype: HT
ms.contentlocale: zh-CN
ms.lasthandoff: 12/25/2019
ms.locfileid: "69583561"
---
# <a name="migrate-from-aspnet-core-21-to-22"></a><span data-ttu-id="16112-103">从 ASP.NET Core 2.1 迁移到 2.2</span><span class="sxs-lookup"><span data-stu-id="16112-103">Migrate from ASP.NET Core 2.1 to 2.2</span></span>

<span data-ttu-id="16112-104">作者：[Scott Addie](https://github.com/scottaddie)</span><span class="sxs-lookup"><span data-stu-id="16112-104">By [Scott Addie](https://github.com/scottaddie)</span></span>

<span data-ttu-id="16112-105">本文介绍如何将现有 ASP.NET Core 2.1 项目更新为 ASP.NET Core 2.2。</span><span class="sxs-lookup"><span data-stu-id="16112-105">This article explains how to update an existing ASP.NET Core 2.1 project to ASP.NET Core 2.2.</span></span>

## <a name="prerequisites"></a><span data-ttu-id="16112-106">先决条件</span><span class="sxs-lookup"><span data-stu-id="16112-106">Prerequisites</span></span>

# <a name="visual-studiotabvisual-studio"></a>[<span data-ttu-id="16112-107">Visual Studio</span><span class="sxs-lookup"><span data-stu-id="16112-107">Visual Studio</span></span>](#tab/visual-studio)

[!INCLUDE[](~/includes/net-core-prereqs-vs2019-2.2.md)]

# <a name="visual-studio-codetabvisual-studio-code"></a>[<span data-ttu-id="16112-108">Visual Studio Code</span><span class="sxs-lookup"><span data-stu-id="16112-108">Visual Studio Code</span></span>](#tab/visual-studio-code)

[!INCLUDE[](~/includes/net-core-prereqs-vsc-2.2.md)]

# <a name="visual-studio-for-mactabvisual-studio-mac"></a>[<span data-ttu-id="16112-109">Visual Studio for Mac</span><span class="sxs-lookup"><span data-stu-id="16112-109">Visual Studio for Mac</span></span>](#tab/visual-studio-mac)

[!INCLUDE[](~/includes/net-core-prereqs-mac-2.2.md)]

---

## <a name="update-target-framework-moniker-tfm"></a><span data-ttu-id="16112-110">更新目标框架名字对象 (TFM)</span><span class="sxs-lookup"><span data-stu-id="16112-110">Update Target Framework Moniker (TFM)</span></span>

<span data-ttu-id="16112-111">面向 .NET Core 的项目需使用 .NET Core 2.2 版或更高版本的 [TFM](/dotnet/standard/frameworks)。</span><span class="sxs-lookup"><span data-stu-id="16112-111">Projects targeting .NET Core should use the [TFM](/dotnet/standard/frameworks) of a version greater than or equal to .NET Core 2.2.</span></span> <span data-ttu-id="16112-112">在项目文件中，通过 `netcoreapp2.2` 更新 `<TargetFramework>` 节点的内部文本：</span><span class="sxs-lookup"><span data-stu-id="16112-112">In the project file, update the `<TargetFramework>` node's inner text with `netcoreapp2.2`:</span></span>

```xml
<TargetFramework>netcoreapp2.2</TargetFramework>
```

<span data-ttu-id="16112-113">面向 .NET Framework 的项目需继续使用 .NET Framework 4.6.1 版或更高版本的 TFM：</span><span class="sxs-lookup"><span data-stu-id="16112-113">Projects targeting .NET Framework may continue to use the TFM of a version greater than or equal to .NET Framework 4.6.1:</span></span>

```xml
<TargetFramework>net461</TargetFramework>
```

## <a name="adopt-the-iis-in-process-hosting-model"></a><span data-ttu-id="16112-114">采用 IIS 进程内托管模型</span><span class="sxs-lookup"><span data-stu-id="16112-114">Adopt the IIS in-process hosting model</span></span>

<span data-ttu-id="16112-115">若要采用 [IIS 进程内托管模型](xref:host-and-deploy/iis/index#in-process-hosting-model)，请将值为 `InProcess` 的 `<AspNetCoreHostingModel>` 属性添加到项目文件中的 `<PropertyGroup>`：</span><span class="sxs-lookup"><span data-stu-id="16112-115">To adopt the [in-process hosting model for IIS](xref:host-and-deploy/iis/index#in-process-hosting-model), add the `<AspNetCoreHostingModel>` property with a value of `InProcess` to a `<PropertyGroup>` in the project file:</span></span>

```xml
<AspNetCoreHostingModel>InProcess</AspNetCoreHostingModel>
```

<span data-ttu-id="16112-116">面向 .NET Framework 的 ASP.NET Core 应用不支持进程内托管模型。</span><span class="sxs-lookup"><span data-stu-id="16112-116">The in-process hosting model isn't supported for ASP.NET Core apps targeting .NET Framework.</span></span>

<span data-ttu-id="16112-117">有关详细信息，请参阅 <xref:host-and-deploy/aspnet-core-module#hosting-models>。</span><span class="sxs-lookup"><span data-stu-id="16112-117">For more information, see <xref:host-and-deploy/aspnet-core-module#hosting-models>.</span></span>

## <a name="update-a-custom-webconfig-file"></a><span data-ttu-id="16112-118">更新自定义 web.config 文件</span><span class="sxs-lookup"><span data-stu-id="16112-118">Update a custom web.config file</span></span>

<span data-ttu-id="16112-119">对于项目根中使用自定义 web.config 文件的项目，要生成发布的 web.config 文件，请执行以下操作   ：</span><span class="sxs-lookup"><span data-stu-id="16112-119">For projects that use a custom *web.config* file in the project root to generate their published *web.config* file:</span></span>

* <span data-ttu-id="16112-120">在添加 ASP.NET Core 模块 (`name="aspNetCore"`) 的 `<handlers>` 项中，将 `modules` 属性值从 `AspNetCoreModule` 更改为 `AspNetCoreModuleV2`。</span><span class="sxs-lookup"><span data-stu-id="16112-120">In the `<handlers>` entry that adds the ASP.NET Core Module (`name="aspNetCore"`), change the `modules` attribute value from `AspNetCoreModule` to `AspNetCoreModuleV2`.</span></span>
* <span data-ttu-id="16112-121">在 `<aspNetCore>` 元素中，添加托管模型特性 (`hostingModel="InProcess"`)。</span><span class="sxs-lookup"><span data-stu-id="16112-121">In the `<aspNetCore>` element, add the hosting model attribute (`hostingModel="InProcess"`).</span></span>

<span data-ttu-id="16112-122">有关详细信息和 web.config 文件的示例，请参阅 <xref:host-and-deploy/aspnet-core-module#configuration-with-webconfig>  。</span><span class="sxs-lookup"><span data-stu-id="16112-122">For more information and example *web.config* files, see <xref:host-and-deploy/aspnet-core-module#configuration-with-webconfig>.</span></span>

## <a name="update-package-references"></a><span data-ttu-id="16112-123">更新包引用</span><span class="sxs-lookup"><span data-stu-id="16112-123">Update package references</span></span>

<span data-ttu-id="16112-124">如果面向 .NET Core，则在项目文件中删除元包引用的 `Version` 特性。</span><span class="sxs-lookup"><span data-stu-id="16112-124">If targeting .NET Core, remove the metapackage reference's `Version` attribute in the project file.</span></span> <span data-ttu-id="16112-125">包含 `Version` 特性会导致出现以下警告：</span><span class="sxs-lookup"><span data-stu-id="16112-125">Inclusion of a `Version` attribute results in the following warning:</span></span>

```console
A PackageReference to 'Microsoft.AspNetCore.App' specified a Version of `2.2.0`. Specifying the version of this package is not recommended. For more information, see https://aka.ms/sdkimplicitrefs
```

<span data-ttu-id="16112-126">有关详细信息，请参阅 <xref:fundamentals/metapackage-app>。</span><span class="sxs-lookup"><span data-stu-id="16112-126">For more information, see <xref:fundamentals/metapackage-app>.</span></span>

<span data-ttu-id="16112-127">元包引用应类似于以下 `<PackageReference />` 节点：</span><span class="sxs-lookup"><span data-stu-id="16112-127">The metapackage reference should resemble the following `<PackageReference />` node:</span></span>

```xml
<ItemGroup>
  <PackageReference Include="Microsoft.AspNetCore.App" />
</ItemGroup>
```

<span data-ttu-id="16112-128">如果面向 .NET Framework，请将每个包引用的 `Version` 特性更新为 2.2.0 或更高版本。</span><span class="sxs-lookup"><span data-stu-id="16112-128">If targeting .NET Framework, update each package reference's `Version` attribute to 2.2.0 or later.</span></span> <span data-ttu-id="16112-129">以下是面向 .NET Framework 的典型 ASP.NET Core 2.2 项目中的包引用：</span><span class="sxs-lookup"><span data-stu-id="16112-129">Here are the package references in a typical ASP.NET Core 2.2 project targeting .NET Framework:</span></span>

```xml
<ItemGroup>
  <PackageReference Include="Microsoft.AspNetCore" Version="2.2.0" />
  <PackageReference Include="Microsoft.AspNetCore.CookiePolicy" Version="2.2.0" />
  <PackageReference Include="Microsoft.AspNetCore.HttpsPolicy" Version="2.2.0" />
  <PackageReference Include="Microsoft.AspNetCore.Mvc" Version="2.2.0" />
  <PackageReference Include="Microsoft.AspNetCore.StaticFiles" Version="2.2.0" />
</ItemGroup>
```

<span data-ttu-id="16112-130">如果引用 [Microsoft.AspNetCore.Razor.Design](https://www.nuget.org/packages/Microsoft.AspNetCore.Razor.Design/) 包，请将其 `Version` 特性更新为 2.2.0 或更高版本。</span><span class="sxs-lookup"><span data-stu-id="16112-130">If referencing the [Microsoft.AspNetCore.Razor.Design](https://www.nuget.org/packages/Microsoft.AspNetCore.Razor.Design/) package, update its `Version` attribute to 2.2.0 or later.</span></span> <span data-ttu-id="16112-131">否则，会导致出现以下错误：</span><span class="sxs-lookup"><span data-stu-id="16112-131">Failure to do so results in the following error:</span></span>

```console
Detected package downgrade: Microsoft.AspNetCore.Razor.Design from 2.2.0 to 2.1.2. Reference the package directly from the project to select a different version.
```

## <a name="update-net-core-sdk-version-in-globaljson"></a><span data-ttu-id="16112-132">在 global.json 中更新 .NET Core SDK 版本</span><span class="sxs-lookup"><span data-stu-id="16112-132">Update .NET Core SDK version in global.json</span></span>

<span data-ttu-id="16112-133">如果解决方案依靠 [global.json](/dotnet/core/tools/global-json) 文件来定向于特定 .NET Core SDK 版本，请将其 `version` 属性更新为计算机上安装的 2.2 版本：</span><span class="sxs-lookup"><span data-stu-id="16112-133">If your solution relies upon a [global.json](/dotnet/core/tools/global-json) file to target a specific .NET Core SDK version, update its `version` property to the 2.2 version installed on your machine:</span></span>

```json
{
  "sdk": {
    "version": "2.2.100"
  }
}
```

## <a name="update-launch-settings"></a><span data-ttu-id="16112-134">更新启动设置</span><span class="sxs-lookup"><span data-stu-id="16112-134">Update launch settings</span></span>

<span data-ttu-id="16112-135">如果使用 Visual Studio Code，请更新项目的启动设置文件 (.vscode/launch.json)  。</span><span class="sxs-lookup"><span data-stu-id="16112-135">If using Visual Studio Code, update the project's launch settings file (*.vscode/launch.json*).</span></span> <span data-ttu-id="16112-136">`program` 路径应引用新 TFM：</span><span class="sxs-lookup"><span data-stu-id="16112-136">The `program` path should reference the new TFM:</span></span>

[!code-json[](21-to-22/samples/launch.json?highlight=9)]

## <a name="update-kestrel-configuration"></a><span data-ttu-id="16112-137">更新 Kestrel 配置</span><span class="sxs-lookup"><span data-stu-id="16112-137">Update Kestrel configuration</span></span>

<span data-ttu-id="16112-138">如果应用通过调用 `Program` 类 [CreateWebHostBuilder 方法](xref:fundamentals/host/web-host#set-up-a-host) 中的 `CreateDefaultBuilder` 来调用 <xref:Microsoft.AspNetCore.Hosting.WebHostBuilderKestrelExtensions.UseKestrel*>，请调用 `ConfigureKestrel` 而非 `UseKestrel` 来配置 Kestrel 服务器，以避免与 [IIS 进程内托管模型](xref:host-and-deploy/iis/index#in-process-hosting-model)发生冲突：</span><span class="sxs-lookup"><span data-stu-id="16112-138">If the app calls <xref:Microsoft.AspNetCore.Hosting.WebHostBuilderKestrelExtensions.UseKestrel*> by calling `CreateDefaultBuilder` in the [CreateWebHostBuilder method](xref:fundamentals/host/web-host#set-up-a-host) of the `Program` class, call `ConfigureKestrel` to configure Kestrel server instead of `UseKestrel` in order to avoid conflicts with the [IIS in-process hosting model](xref:host-and-deploy/iis/index#in-process-hosting-model):</span></span>

```csharp
public static IWebHostBuilder CreateWebHostBuilder(string[] args) =>
    WebHost.CreateDefaultBuilder(args)
        .UseStartup<Startup>()
        .ConfigureKestrel((context, options) =>
        {
            // Set properties and call methods on options
        });
```

<span data-ttu-id="16112-139">如果应用不调用 `CreateDefaultBuilder` 并在 `Program` 类中手动生成主机，请在调用 `ConfigureKestrel`之前调用 <xref:Microsoft.AspNetCore.Hosting.WebHostBuilderKestrelExtensions.UseKestrel*>  ：</span><span class="sxs-lookup"><span data-stu-id="16112-139">If the app doesn't call `CreateDefaultBuilder` and builds the host manually in the `Program` class, call <xref:Microsoft.AspNetCore.Hosting.WebHostBuilderKestrelExtensions.UseKestrel*> **before** calling `ConfigureKestrel`:</span></span>

```csharp
public static void Main(string[] args)
{
    var host = new WebHostBuilder()
        .UseContentRoot(Directory.GetCurrentDirectory())
        .UseKestrel()
        .UseIISIntegration()
        .UseStartup<Startup>()
        .ConfigureKestrel((context, options) =>
        {
            // Set properties and call methods on options
        })
        .Build();

    host.Run();
}
```

<span data-ttu-id="16112-140">有关详细信息，请参阅 <xref:fundamentals/servers/kestrel#how-to-use-kestrel-in-aspnet-core-apps>。</span><span class="sxs-lookup"><span data-stu-id="16112-140">For more information, see <xref:fundamentals/servers/kestrel#how-to-use-kestrel-in-aspnet-core-apps>.</span></span>

## <a name="update-compatibility-version"></a><span data-ttu-id="16112-141">更新兼容性版本</span><span class="sxs-lookup"><span data-stu-id="16112-141">Update compatibility version</span></span>

<span data-ttu-id="16112-142">将 `Startup.ConfigureServices` 中的兼容性版本更新为 `Version_2_2`：</span><span class="sxs-lookup"><span data-stu-id="16112-142">Update the compatibility version in `Startup.ConfigureServices` to `Version_2_2`:</span></span>

```csharp
services.AddMvc()
        .SetCompatibilityVersion(CompatibilityVersion.Version_2_2);
```

## <a name="update-cors-policy"></a><span data-ttu-id="16112-143">更新 CORS 策略</span><span class="sxs-lookup"><span data-stu-id="16112-143">Update CORS policy</span></span>

<span data-ttu-id="16112-144">在 ASP.NET Core 2.2 中，如果策略允许任何来源并允许凭据，则 CORS 中间件会使用通配符源 (`*`) 进行响应。</span><span class="sxs-lookup"><span data-stu-id="16112-144">In ASP.NET Core 2.2, the CORS middleware responds with a wildcard origin (`*`) if a policy allows any origin and allows credentials.</span></span> <span data-ttu-id="16112-145">如果指定通配符源 (`*`)，则不支持凭据，并且浏览器将不允许 CORS 请求。</span><span class="sxs-lookup"><span data-stu-id="16112-145">Credentials aren't supported when a wildcard origin (`*`) is specified, and browsers will disallow the CORS request.</span></span> <span data-ttu-id="16112-146">有关详细信息，包括如何在客户端解决此问题的选项，请参阅 [MDN Web 文档](https://developer.mozilla.org/docs/Web/HTTP/CORS/Errors/CORSNotSupportingCredentials)。</span><span class="sxs-lookup"><span data-stu-id="16112-146">For more information, including options for correcting the problem on the client, see the [MDN web docs](https://developer.mozilla.org/docs/Web/HTTP/CORS/Errors/CORSNotSupportingCredentials).</span></span>

<span data-ttu-id="16112-147">若要在服务器上解决此问题，请执行下列操作之一：</span><span class="sxs-lookup"><span data-stu-id="16112-147">To correct this problem on the server, take one of the following actions:</span></span>

* <span data-ttu-id="16112-148">修改 CORS 策略以不再允许凭据。</span><span class="sxs-lookup"><span data-stu-id="16112-148">Modify the CORS policy to no longer allow credentials.</span></span> <span data-ttu-id="16112-149">也就是说，在配置策略时，删除对 <xref:Microsoft.AspNetCore.Cors.Infrastructure.CorsPolicyBuilder.AllowCredentials*> 的调用。</span><span class="sxs-lookup"><span data-stu-id="16112-149">That is, remove the call to <xref:Microsoft.AspNetCore.Cors.Infrastructure.CorsPolicyBuilder.AllowCredentials*> when configuring the policy.</span></span>
* <span data-ttu-id="16112-150">如果 CORS 请求需要提供凭据才能成功，请修改策略以指定允许的主机。</span><span class="sxs-lookup"><span data-stu-id="16112-150">If credentials are required for the CORS request to succeed, modify the policy to specify allowed hosts.</span></span> <span data-ttu-id="16112-151">例如，请使用 `builder.WithOrigins("https://api.example1.com", "https://example2.com")` 而不是使用 <xref:Microsoft.AspNetCore.Cors.Infrastructure.CorsPolicyBuilder.AllowAnyOrigin*>。</span><span class="sxs-lookup"><span data-stu-id="16112-151">For example, use `builder.WithOrigins("https://api.example1.com", "https://example2.com")` instead of using <xref:Microsoft.AspNetCore.Cors.Infrastructure.CorsPolicyBuilder.AllowAnyOrigin*>.</span></span>

## <a name="update-docker-images"></a><span data-ttu-id="16112-152">更新 Docker 映像</span><span class="sxs-lookup"><span data-stu-id="16112-152">Update Docker images</span></span>

<span data-ttu-id="16112-153">下表显示了 Docker 映像标记更改：</span><span class="sxs-lookup"><span data-stu-id="16112-153">The following table shows the Docker image tag changes:</span></span>

| <span data-ttu-id="16112-154">2.1</span><span class="sxs-lookup"><span data-stu-id="16112-154">2.1</span></span>                                       | <span data-ttu-id="16112-155">2.2</span><span class="sxs-lookup"><span data-stu-id="16112-155">2.2</span></span>                                        |
| ----------------------------------------- | -----------------------------------------  |
| `microsoft/dotnet:2.1-aspnetcore-runtime` | `mcr.microsoft.com/dotnet/core/aspnet:2.2` |
| `microsoft/dotnet:2.1-sdk`                | `mcr.microsoft.com/dotnet/core/sdk:2.2`    |

<span data-ttu-id="16112-156">将 Dockerfile 中的 `FROM` 行更改为使用上表的 2.2 列中的新映像标记  。</span><span class="sxs-lookup"><span data-stu-id="16112-156">Change the `FROM` lines in your *Dockerfile* to use the new image tags in the preceding table's 2.2 column.</span></span>

## <a name="build-manually-in-visual-studio-when-using-iis-in-process-hosting"></a><span data-ttu-id="16112-157">使用 IIS 进程内托管时，在 Visual Studio 中手动生成</span><span class="sxs-lookup"><span data-stu-id="16112-157">Build manually in Visual Studio when using IIS in-process hosting</span></span>

<span data-ttu-id="16112-158">Visual Studio 的“在浏览器请求时自动生成”体验无法与 [IIS 进程内托管模型](xref:host-and-deploy/iis/index#in-process-hosting-model)一起使用  。</span><span class="sxs-lookup"><span data-stu-id="16112-158">Visual Studio's **Auto build on browser request** experience doesn't function with the [IIS in-process hosting model](xref:host-and-deploy/iis/index#in-process-hosting-model).</span></span> <span data-ttu-id="16112-159">使用进程内托管时，必须手动重新生成项目。</span><span class="sxs-lookup"><span data-stu-id="16112-159">You must manually rebuild the project when using in-process hosting.</span></span> <span data-ttu-id="16112-160">我们已计划对 Visual Studio 的未来版本改进此体验。</span><span class="sxs-lookup"><span data-stu-id="16112-160">Improvements to this experience are planned for a future release of Visual Studio.</span></span>

## <a name="update-logging-code"></a><span data-ttu-id="16112-161">更新日志记录代码</span><span class="sxs-lookup"><span data-stu-id="16112-161">Update logging code</span></span>

<span data-ttu-id="16112-162">建议的日志记录配置代码未从 2.1 更改为 2.2，但在 2.1 中仍然有效的某些 1.x 编码模式在 2.2 中不再适用。</span><span class="sxs-lookup"><span data-stu-id="16112-162">Recommended logging configuration code didn't change from 2.1 to 2.2, but some 1.x coding patterns that still worked in 2.1 no longer work in 2.2.</span></span>

<span data-ttu-id="16112-163">如果应用在 `Startup` 类中执行日志记录提供程序初始化、筛选和配置加载，请将该代码移动到 `Program.Main`：</span><span class="sxs-lookup"><span data-stu-id="16112-163">If your app does logging provider initialization, filtering, and configuration loading in the `Startup` class, move that code to `Program.Main`:</span></span>

* <span data-ttu-id="16112-164">提供程序初始化：</span><span class="sxs-lookup"><span data-stu-id="16112-164">Provider initialization:</span></span>

  <span data-ttu-id="16112-165">1.x 示例：</span><span class="sxs-lookup"><span data-stu-id="16112-165">1.x example:</span></span>

  ```csharp
  public void Configure(IApplicationBuilder app, ILoggerFactory loggerFactory)
  {
      loggerFactory.AddConsole();
  }
  ```

  <span data-ttu-id="16112-166">2.2 示例：</span><span class="sxs-lookup"><span data-stu-id="16112-166">2.2 example:</span></span>

  ```csharp

  public static void Main(string[] args)
  {
      var webHost = new WebHostBuilder()
          // ...
          .ConfigureLogging((hostingContext, logging) =>
          {
              logging.AddConsole();
          })
          // ...
  }
  ```

* <span data-ttu-id="16112-167">筛选：</span><span class="sxs-lookup"><span data-stu-id="16112-167">Filtering:</span></span>

  <span data-ttu-id="16112-168">1.x 示例：</span><span class="sxs-lookup"><span data-stu-id="16112-168">1.x example:</span></span>

  ```csharp
  public void Configure(IApplicationBuilder app, ILoggerFactory loggerFactory)
  {
      loggerFactory.AddConsole(LogLevel.Information);
      // or
      loggerFactory.AddConsole((category, level) => 
          category == "A" || level == LogLevel.Critical);
  }
  ```

  <span data-ttu-id="16112-169">2.2 示例：</span><span class="sxs-lookup"><span data-stu-id="16112-169">2.2 example:</span></span>

  ```csharp
  public static void Main(string[] args)
  {
      var webHost = new WebHostBuilder()
          // ...
          .ConfigureLogging((hostingContext, logging) =>
          {
              logging.AddConsole()
                     .AddFilter<ConsoleLoggerProvider>
                         (category: null, level: LogLevel.Information)
                     // or
                     .AddFilter<ConsoleLoggerProvider>
                         ((category, level) => category == "A" ||
                             level == LogLevel.Critical)
              );
          })
          // ...
  }
  ```

* <span data-ttu-id="16112-170">配置加载：</span><span class="sxs-lookup"><span data-stu-id="16112-170">Configuration loading:</span></span>

  <span data-ttu-id="16112-171">1.x 示例：</span><span class="sxs-lookup"><span data-stu-id="16112-171">1.x example:</span></span>

  ```csharp
  public void Configure(IApplicationBuilder app, ILoggerFactory loggerFactory)
  {
      loggerFactory.AddConsole(Configuration);
  }
  ```

  <span data-ttu-id="16112-172">2.2 示例：</span><span class="sxs-lookup"><span data-stu-id="16112-172">2.2 example:</span></span>

  ```csharp
  public static void Main(string[] args)
  {
      var webHost = new WebHostBuilder()
          // ...
          .ConfigureLogging((hostingContext, logging) =>
          {
              logging.AddConfiguration(hostingContext.Configuration.GetSection("Logging"));
              logging.AddConsole();
          })
          // ...
  }
  ```

<span data-ttu-id="16112-173">有关详细信息，请参阅<xref:fundamentals/logging/index>。</span><span class="sxs-lookup"><span data-stu-id="16112-173">For more information, see <xref:fundamentals/logging/index></span></span>

## <a name="additional-resources"></a><span data-ttu-id="16112-174">其他资源</span><span class="sxs-lookup"><span data-stu-id="16112-174">Additional resources</span></span>

* <xref:mvc/compatibility-version>
* <xref:fundamentals/metapackage-app>
* [<span data-ttu-id="16112-175">隐式包引用</span><span class="sxs-lookup"><span data-stu-id="16112-175">Implicit package references</span></span>](/dotnet/core/tools/csproj#implicit-package-references)
