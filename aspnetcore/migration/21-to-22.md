---
title: 从 ASP.NET Core 2.1 迁移到 2.2
author: scottaddie
description: 本文介绍如何将现有的 ASP.NET Core 2.1 项目更新到 ASP.NET Core 2.2。
ms.author: scaddie
ms.custom: mvc
ms.date: 05/28/2019
uid: migration/21-to-22
ms.openlocfilehash: 5b26b9c5b00d44c0bb5f52caece247dd59a73e5d
ms.sourcegitcommit: 335a88c1b6e7f0caa8a3a27db57c56664d676d34
ms.translationtype: MT
ms.contentlocale: zh-CN
ms.lasthandoff: 06/12/2019
ms.locfileid: "67034874"
---
# <a name="migrate-from-aspnet-core-21-to-22"></a><span data-ttu-id="829b6-103">从 ASP.NET Core 2.1 迁移到 2.2</span><span class="sxs-lookup"><span data-stu-id="829b6-103">Migrate from ASP.NET Core 2.1 to 2.2</span></span>

<span data-ttu-id="829b6-104">作者：[Scott Addie](https://github.com/scottaddie)</span><span class="sxs-lookup"><span data-stu-id="829b6-104">By [Scott Addie](https://github.com/scottaddie)</span></span>

<span data-ttu-id="829b6-105">本文介绍如何将现有的 ASP.NET Core 2.1 项目更新到 ASP.NET Core 2.2。</span><span class="sxs-lookup"><span data-stu-id="829b6-105">This article explains how to update an existing ASP.NET Core 2.1 project to ASP.NET Core 2.2.</span></span>

[!INCLUDE[](~/includes/net-core-prereqs-all-2.2.md)]

## <a name="update-target-framework-moniker-tfm"></a><span data-ttu-id="829b6-106">更新目标框架名字对象 (TFM)</span><span class="sxs-lookup"><span data-stu-id="829b6-106">Update Target Framework Moniker (TFM)</span></span>

<span data-ttu-id="829b6-107">面向 .NET Core 的项目应使用版本大于或等于 .NET Core 2.2 的 [TFM](/dotnet/standard/frameworks#referring-to-frameworks)。</span><span class="sxs-lookup"><span data-stu-id="829b6-107">Projects targeting .NET Core should use the [TFM](/dotnet/standard/frameworks#referring-to-frameworks) of a version greater than or equal to .NET Core 2.2.</span></span> <span data-ttu-id="829b6-108">在项目文件中，更新`<TargetFramework>`节点的内部文本替换`netcoreapp2.2`:</span><span class="sxs-lookup"><span data-stu-id="829b6-108">In the project file, update the `<TargetFramework>` node's inner text with `netcoreapp2.2`:</span></span>

```xml
<TargetFramework>netcoreapp2.2</TargetFramework>
```

<span data-ttu-id="829b6-109">面向.NET Framework 的项目可以继续使用大于或等于.NET Framework 4.6.1 版本的 TFM:</span><span class="sxs-lookup"><span data-stu-id="829b6-109">Projects targeting .NET Framework may continue to use the TFM of a version greater than or equal to .NET Framework 4.6.1:</span></span>

```xml
<TargetFramework>net461</TargetFramework>
```

## <a name="adopt-the-iis-in-process-hosting-model"></a><span data-ttu-id="829b6-110">采用的 IIS 进程内承载模型</span><span class="sxs-lookup"><span data-stu-id="829b6-110">Adopt the IIS in-process hosting model</span></span>

<span data-ttu-id="829b6-111">若要采用[IIS 进程内承载模型](xref:host-and-deploy/iis/index#in-process-hosting-model)，添加`<AspNetCoreHostingModel>`属性值为`InProcess`到`<PropertyGroup>`项目文件中：</span><span class="sxs-lookup"><span data-stu-id="829b6-111">To adopt the [in-process hosting model for IIS](xref:host-and-deploy/iis/index#in-process-hosting-model), add the `<AspNetCoreHostingModel>` property with a value of `InProcess` to a `<PropertyGroup>` in the project file:</span></span>

```xml
<AspNetCoreHostingModel>InProcess</AspNetCoreHostingModel>
```

<span data-ttu-id="829b6-112">对于面向.NET Framework 的 ASP.NET Core 应用程序不支持进程内承载模型。</span><span class="sxs-lookup"><span data-stu-id="829b6-112">The in-process hosting model isn't supported for ASP.NET Core apps targeting .NET Framework.</span></span>

<span data-ttu-id="829b6-113">有关详细信息，请参阅 <xref:host-and-deploy/aspnet-core-module#hosting-models>。</span><span class="sxs-lookup"><span data-stu-id="829b6-113">For more information, see <xref:host-and-deploy/aspnet-core-module#hosting-models>.</span></span>

## <a name="update-a-custom-webconfig-file"></a><span data-ttu-id="829b6-114">更新自定义 web.config 文件</span><span class="sxs-lookup"><span data-stu-id="829b6-114">Update a custom web.config file</span></span>

<span data-ttu-id="829b6-115">对于使用自定义项目*web.config*文件中要生成其已发布的项目根*web.config*文件：</span><span class="sxs-lookup"><span data-stu-id="829b6-115">For projects that use a custom *web.config* file in the project root to generate their published *web.config* file:</span></span>

* <span data-ttu-id="829b6-116">在中`<handlers>`添加了 ASP.NET Core 模块的条目 (`name="aspNetCore"`)，更改`modules`属性值从`AspNetCoreModule`到`AspNetCoreModuleV2`。</span><span class="sxs-lookup"><span data-stu-id="829b6-116">In the `<handlers>` entry that adds the ASP.NET Core Module (`name="aspNetCore"`), change the `modules` attribute value from `AspNetCoreModule` to `AspNetCoreModuleV2`.</span></span>
* <span data-ttu-id="829b6-117">在中`<aspNetCore>`元素中，添加托管的模型属性 (`hostingModel="InProcess"`)。</span><span class="sxs-lookup"><span data-stu-id="829b6-117">In the `<aspNetCore>` element, add the hosting model attribute (`hostingModel="InProcess"`).</span></span>

<span data-ttu-id="829b6-118">有关详细信息和示例*web.config*文件，请参阅<xref:host-and-deploy/aspnet-core-module#configuration-with-webconfig>。</span><span class="sxs-lookup"><span data-stu-id="829b6-118">For more information and example *web.config* files, see <xref:host-and-deploy/aspnet-core-module#configuration-with-webconfig>.</span></span>

## <a name="update-package-references"></a><span data-ttu-id="829b6-119">更新包引用</span><span class="sxs-lookup"><span data-stu-id="829b6-119">Update package references</span></span>

<span data-ttu-id="829b6-120">如果定目标到.NET Core，删除元包引用`Version`项目文件中的属性。</span><span class="sxs-lookup"><span data-stu-id="829b6-120">If targeting .NET Core, remove the metapackage reference's `Version` attribute in the project file.</span></span> <span data-ttu-id="829b6-121">包含 `Version` 属性将导致以下警告：</span><span class="sxs-lookup"><span data-stu-id="829b6-121">Inclusion of a `Version` attribute results in the following warning:</span></span>

```console
A PackageReference to 'Microsoft.AspNetCore.App' specified a Version of `2.2.0`. Specifying the version of this package is not recommended. For more information, see https://aka.ms/sdkimplicitrefs
```

<span data-ttu-id="829b6-122">有关详细信息，请参阅 <xref:fundamentals/metapackage-app>。</span><span class="sxs-lookup"><span data-stu-id="829b6-122">For more information, see <xref:fundamentals/metapackage-app>.</span></span>

<span data-ttu-id="829b6-123">元包引用应与下方的 `<PackageReference />` 节点相似：</span><span class="sxs-lookup"><span data-stu-id="829b6-123">The metapackage reference should resemble the following `<PackageReference />` node:</span></span>

```xml
<ItemGroup>
  <PackageReference Include="Microsoft.AspNetCore.App" />
</ItemGroup>
```

<span data-ttu-id="829b6-124">如果面向 .NET Framework，请将每个包引用的 `Version` 属性更新到 2.2.0 或更高版本。</span><span class="sxs-lookup"><span data-stu-id="829b6-124">If targeting .NET Framework, update each package reference's `Version` attribute to 2.2.0 or later.</span></span> <span data-ttu-id="829b6-125">下面是面向 .NET Framework 的典型 ASP.NET Core 2.2 项目中的包引用：</span><span class="sxs-lookup"><span data-stu-id="829b6-125">Here are the package references in a typical ASP.NET Core 2.2 project targeting .NET Framework:</span></span>

```xml
<ItemGroup>
  <PackageReference Include="Microsoft.AspNetCore" Version="2.2.0" />
  <PackageReference Include="Microsoft.AspNetCore.CookiePolicy" Version="2.2.0" />
  <PackageReference Include="Microsoft.AspNetCore.HttpsPolicy" Version="2.2.0" />
  <PackageReference Include="Microsoft.AspNetCore.Mvc" Version="2.2.0" />
  <PackageReference Include="Microsoft.AspNetCore.StaticFiles" Version="2.2.0" />
</ItemGroup>
```

<span data-ttu-id="829b6-126">如果在引用[Microsoft.AspNetCore.Razor.Design](https://www.nuget.org/packages/Microsoft.AspNetCore.Razor.Design/)包，更新其`Version`属性到 2.2.0 或更高版本。</span><span class="sxs-lookup"><span data-stu-id="829b6-126">If referencing the [Microsoft.AspNetCore.Razor.Design](https://www.nuget.org/packages/Microsoft.AspNetCore.Razor.Design/) package, update its `Version` attribute to 2.2.0 or later.</span></span> <span data-ttu-id="829b6-127">如果不这样做会导致以下错误：</span><span class="sxs-lookup"><span data-stu-id="829b6-127">Failure to do so results in the following error:</span></span>

```console
Detected package downgrade: Microsoft.AspNetCore.Razor.Design from 2.2.0 to 2.1.2. Reference the package directly from the project to select a different version.
```

## <a name="update-net-core-sdk-version-in-globaljson"></a><span data-ttu-id="829b6-128">在 global.json 中更新 .NET Core SDK 版本</span><span class="sxs-lookup"><span data-stu-id="829b6-128">Update .NET Core SDK version in global.json</span></span>

<span data-ttu-id="829b6-129">如果解决方案依靠 [global.json](/dotnet/core/tools/global-json) 文件来面向特定 .NET Core SDK 的版本，请将其 `version` 属性更新为你的计算机上安装的 2.2 版本：</span><span class="sxs-lookup"><span data-stu-id="829b6-129">If your solution relies upon a [global.json](/dotnet/core/tools/global-json) file to target a specific .NET Core SDK version, update its `version` property to the 2.2 version installed on your machine:</span></span>

```json
{
  "sdk": {
    "version": "2.2.100"
  }
}
```

## <a name="update-launch-settings"></a><span data-ttu-id="829b6-130">更新启动设置</span><span class="sxs-lookup"><span data-stu-id="829b6-130">Update launch settings</span></span>

<span data-ttu-id="829b6-131">如果使用 Visual Studio Code 中更新项目的启动设置文件 ( *.vscode/launch.json*)。</span><span class="sxs-lookup"><span data-stu-id="829b6-131">If using Visual Studio Code, update the project's launch settings file (*.vscode/launch.json*).</span></span> <span data-ttu-id="829b6-132">`program`路径应引用的新 TFM:</span><span class="sxs-lookup"><span data-stu-id="829b6-132">The `program` path should reference the new TFM:</span></span>

[!code-json[](21-to-22/samples/launch.json?highlight=9)]

## <a name="update-kestrel-configuration"></a><span data-ttu-id="829b6-133">更新 Kestrel 配置</span><span class="sxs-lookup"><span data-stu-id="829b6-133">Update Kestrel configuration</span></span>

<span data-ttu-id="829b6-134">如果该应用程序调用<xref:Microsoft.AspNetCore.Hosting.WebHostBuilderKestrelExtensions.UseKestrel*>通过调用`CreateDefaultBuilder`中[CreateWebHostBuilder 方法](xref:fundamentals/host/web-host#set-up-a-host)的`Program`类中，调用`ConfigureKestrel`配置 Kestrel 服务器，而不是`UseKestrel`以避免与冲突[IIS 进程托管模型](xref:host-and-deploy/iis/index#in-process-hosting-model):</span><span class="sxs-lookup"><span data-stu-id="829b6-134">If the app calls <xref:Microsoft.AspNetCore.Hosting.WebHostBuilderKestrelExtensions.UseKestrel*> by calling `CreateDefaultBuilder` in the [CreateWebHostBuilder method](xref:fundamentals/host/web-host#set-up-a-host) of the `Program` class, call `ConfigureKestrel` to configure Kestrel server instead of `UseKestrel` in order to avoid conflicts with the [IIS in-process hosting model](xref:host-and-deploy/iis/index#in-process-hosting-model):</span></span>

```csharp
public static IWebHostBuilder CreateWebHostBuilder(string[] args) =>
    WebHost.CreateDefaultBuilder(args)
        .UseStartup<Startup>()
        .ConfigureKestrel((context, options) =>
        {
            // Set properties and call methods on options
        });
```

<span data-ttu-id="829b6-135">如果应用程序不会调用`CreateDefaultBuilder`，并生成主机中手动`Program`类中，调用<xref:Microsoft.AspNetCore.Hosting.WebHostBuilderKestrelExtensions.UseKestrel*>**之前**调用`ConfigureKestrel`:</span><span class="sxs-lookup"><span data-stu-id="829b6-135">If the app doesn't call `CreateDefaultBuilder` and builds the host manually in the `Program` class, call <xref:Microsoft.AspNetCore.Hosting.WebHostBuilderKestrelExtensions.UseKestrel*> **before** calling `ConfigureKestrel`:</span></span>

```csharp
public static void Main(string[] args)
{
    var host = new WebHostBuilder()
        .UseContentRoot(Directory.GetCurrentDirectory())
        .UseKestrel()
        .UseIISIntegration()
        .UseStartup<Startup>()
        .ConfigureKestrel((context, options) =>
        {
            // Set properties and call methods on options
        })
        .Build();

    host.Run();
}
```

<span data-ttu-id="829b6-136">有关详细信息，请参阅 <xref:fundamentals/servers/kestrel#how-to-use-kestrel-in-aspnet-core-apps>。</span><span class="sxs-lookup"><span data-stu-id="829b6-136">For more information, see <xref:fundamentals/servers/kestrel#how-to-use-kestrel-in-aspnet-core-apps>.</span></span>

## <a name="update-compatibility-version"></a><span data-ttu-id="829b6-137">更新兼容性版本</span><span class="sxs-lookup"><span data-stu-id="829b6-137">Update compatibility version</span></span>

<span data-ttu-id="829b6-138">将 `Startup.ConfigureServices` 中的兼容性版本更新为 `Version_2_2`：</span><span class="sxs-lookup"><span data-stu-id="829b6-138">Update the compatibility version in `Startup.ConfigureServices` to `Version_2_2`:</span></span>

```csharp
services.AddMvc()
        .SetCompatibilityVersion(CompatibilityVersion.Version_2_2);
```

## <a name="update-cors-policy"></a><span data-ttu-id="829b6-139">更新 CORS 策略</span><span class="sxs-lookup"><span data-stu-id="829b6-139">Update CORS policy</span></span>

<span data-ttu-id="829b6-140">ASP.NET Core 2.2 中使用通配符源的进行响应 CORS 中间件 (`*`) 如果策略允许任何源，并允许凭据。</span><span class="sxs-lookup"><span data-stu-id="829b6-140">In ASP.NET Core 2.2, the CORS middleware responds with a wildcard origin (`*`) if a policy allows any origin and allows credentials.</span></span> <span data-ttu-id="829b6-141">凭据不支持通配符源时 (`*`) 指定，则，浏览器将不允许 CORS 请求。</span><span class="sxs-lookup"><span data-stu-id="829b6-141">Credentials aren't supported when a wildcard origin (`*`) is specified, and browsers will disallow the CORS request.</span></span> <span data-ttu-id="829b6-142">有关详细信息，包括用于更正该问题在客户端，请参阅[MDN web 文档](https://developer.mozilla.org/docs/Web/HTTP/CORS/Errors/CORSNotSupportingCredentials)。</span><span class="sxs-lookup"><span data-stu-id="829b6-142">For more information, including options for correcting the problem on the client, see the [MDN web docs](https://developer.mozilla.org/docs/Web/HTTP/CORS/Errors/CORSNotSupportingCredentials).</span></span>

<span data-ttu-id="829b6-143">若要更正此问题在服务器上的，执行以下操作之一：</span><span class="sxs-lookup"><span data-stu-id="829b6-143">To correct this problem on the server, take one of the following actions:</span></span>

* <span data-ttu-id="829b6-144">修改要不再允许凭据的 CORS 策略。</span><span class="sxs-lookup"><span data-stu-id="829b6-144">Modify the CORS policy to no longer allow credentials.</span></span> <span data-ttu-id="829b6-145">也就是说，删除对调用<xref:Microsoft.AspNetCore.Cors.Infrastructure.CorsPolicyBuilder.AllowCredentials*>时配置的策略。</span><span class="sxs-lookup"><span data-stu-id="829b6-145">That is, remove the call to <xref:Microsoft.AspNetCore.Cors.Infrastructure.CorsPolicyBuilder.AllowCredentials*> when configuring the policy.</span></span>
* <span data-ttu-id="829b6-146">如果凭据所需的 CORS 请求成功执行，修改策略来指定允许的主机。</span><span class="sxs-lookup"><span data-stu-id="829b6-146">If credentials are required for the CORS request to succeed, modify the policy to specify allowed hosts.</span></span> <span data-ttu-id="829b6-147">例如，使用`builder.WithOrigins("https://api.example1.com", "https://example2.com")`而不是使用<xref:Microsoft.AspNetCore.Cors.Infrastructure.CorsPolicyBuilder.AllowAnyOrigin*>。</span><span class="sxs-lookup"><span data-stu-id="829b6-147">For example, use `builder.WithOrigins("https://api.example1.com", "https://example2.com")` instead of using <xref:Microsoft.AspNetCore.Cors.Infrastructure.CorsPolicyBuilder.AllowAnyOrigin*>.</span></span>

## <a name="update-docker-images"></a><span data-ttu-id="829b6-148">更新 Docker 映像</span><span class="sxs-lookup"><span data-stu-id="829b6-148">Update Docker images</span></span>

<span data-ttu-id="829b6-149">下表显示了 Docker 映像标记更改：</span><span class="sxs-lookup"><span data-stu-id="829b6-149">The following table shows the Docker image tag changes:</span></span>

| <span data-ttu-id="829b6-150">2.1</span><span class="sxs-lookup"><span data-stu-id="829b6-150">2.1</span></span>                                       | <span data-ttu-id="829b6-151">2.2</span><span class="sxs-lookup"><span data-stu-id="829b6-151">2.2</span></span>                                       |
| ----------------------------------------- | ----------------------------------------- |
| `microsoft/dotnet:2.1-aspnetcore-runtime` | `microsoft/dotnet:2.2-aspnetcore-runtime` |
| `microsoft/dotnet:2.1-sdk`                | `microsoft/dotnet:2.2-sdk`                |

<span data-ttu-id="829b6-152">更改`FROM`行中您*Dockerfile*以使用上表中的 2.2 列中的新图像标记。</span><span class="sxs-lookup"><span data-stu-id="829b6-152">Change the `FROM` lines in your *Dockerfile* to use the new image tags in the preceding table's 2.2 column.</span></span>

## <a name="build-manually-in-visual-studio-when-using-iis-in-process-hosting"></a><span data-ttu-id="829b6-153">使用 IIS 进程内承载时，手动构建 Visual Studio 中</span><span class="sxs-lookup"><span data-stu-id="829b6-153">Build manually in Visual Studio when using IIS in-process hosting</span></span>

<span data-ttu-id="829b6-154">Visual Studio**浏览器请求的自动生成**不起作用体验[IIS 进程托管模型](xref:host-and-deploy/iis/index#in-process-hosting-model)。</span><span class="sxs-lookup"><span data-stu-id="829b6-154">Visual Studio's **Auto build on browser request** experience doesn't function with the [IIS in-process hosting model](xref:host-and-deploy/iis/index#in-process-hosting-model).</span></span> <span data-ttu-id="829b6-155">使用进程内承载时，必须手动重新生成项目。</span><span class="sxs-lookup"><span data-stu-id="829b6-155">You must manually rebuild the project when using in-process hosting.</span></span> <span data-ttu-id="829b6-156">此体验的改进计划未来版本的 Visual Studio。</span><span class="sxs-lookup"><span data-stu-id="829b6-156">Improvements to this experience are planned for a future release of Visual Studio.</span></span>

## <a name="update-logging-code"></a><span data-ttu-id="829b6-157">更新日志记录代码</span><span class="sxs-lookup"><span data-stu-id="829b6-157">Update logging code</span></span>

<span data-ttu-id="829b6-158">建议的日志记录配置代码没有从 2.1 更改为 2.2，但在 2.2 中仍不能再处理 2.1 中某些 1.x 编码模式工作。</span><span class="sxs-lookup"><span data-stu-id="829b6-158">Recommended logging configuration code didn't change from 2.1 to 2.2, but some 1.x coding patterns that still worked in 2.1 no longer work in 2.2.</span></span>

<span data-ttu-id="829b6-159">如果您的应用程序日志记录提供程序初始化、 筛选和配置中加载`Startup`类中，将移动到该代码`Program.Main`:</span><span class="sxs-lookup"><span data-stu-id="829b6-159">If your app does logging provider initialization, filtering, and configuration loading in the `Startup` class, move that code to `Program.Main`:</span></span>

* <span data-ttu-id="829b6-160">提供程序初始化：</span><span class="sxs-lookup"><span data-stu-id="829b6-160">Provider initialization:</span></span>

  <span data-ttu-id="829b6-161">1.x 示例：</span><span class="sxs-lookup"><span data-stu-id="829b6-161">1.x example:</span></span>

  ```csharp
  public void Configure(IApplicationBuilder app, ILoggerFactory loggerFactory)
  {
      loggerFactory.AddConsole();
  }
  ```

  <span data-ttu-id="829b6-162">2.2 示例：</span><span class="sxs-lookup"><span data-stu-id="829b6-162">2.2 example:</span></span>

  ```csharp

  public static void Main(string[] args)
  {
      var webHost = new WebHostBuilder()
          // ...
          .ConfigureLogging((hostingContext, logging) =>
          {
              logging.AddConsole();
          })
          // ...
  }
  ```

* <span data-ttu-id="829b6-163">筛选：</span><span class="sxs-lookup"><span data-stu-id="829b6-163">Filtering:</span></span>

  <span data-ttu-id="829b6-164">1.x 示例：</span><span class="sxs-lookup"><span data-stu-id="829b6-164">1.x example:</span></span>

  ```csharp
  public void Configure(IApplicationBuilder app, ILoggerFactory loggerFactory)
  {
      loggerFactory.AddConsole(LogLevel.Information);
      // or
      loggerFactory.AddConsole((category, level) => 
          category == "A" || level == LogLevel.Critical);
  }
  ```

  <span data-ttu-id="829b6-165">2.2 示例：</span><span class="sxs-lookup"><span data-stu-id="829b6-165">2.2 example:</span></span>

  ```csharp
  public static void Main(string[] args)
  {
      var webHost = new WebHostBuilder()
          // ...
          .ConfigureLogging((hostingContext, logging) =>
          {
              logging.AddConsole()
                     .AddFilter<ConsoleLoggerProvider>
                         (category: null, level: LogLevel.Information)
                     // or
                     .AddFilter<ConsoleLoggerProvider>
                         ((category, level) => category == "A" ||
                             level == LogLevel.Critical)
              );
          })
          // ...
  }
  ```

* <span data-ttu-id="829b6-166">配置加载：</span><span class="sxs-lookup"><span data-stu-id="829b6-166">Configuration loading:</span></span>

  <span data-ttu-id="829b6-167">1.x 示例：</span><span class="sxs-lookup"><span data-stu-id="829b6-167">1.x example:</span></span>

  ```csharp
  public void Configure(IApplicationBuilder app, ILoggerFactory loggerFactory)
  {
      loggerFactory.AddConsole(Configuration);
  }
  ```

  <span data-ttu-id="829b6-168">2.2 示例：</span><span class="sxs-lookup"><span data-stu-id="829b6-168">2.2 example:</span></span>

  ```csharp
  public static void Main(string[] args)
  {
      var webHost = new WebHostBuilder()
          // ...
          .ConfigureLogging((hostingContext, logging) =>
          {
              logging.AddConfiguration(hostingContext.Configuration.GetSection("Logging"));
              logging.AddConsole();
          })
          // ...
  }
  ```

<span data-ttu-id="829b6-169">有关详细信息，请参阅<xref:fundamentals/logging/index>。</span><span class="sxs-lookup"><span data-stu-id="829b6-169">For more information, see <xref:fundamentals/logging/index></span></span>

## <a name="additional-resources"></a><span data-ttu-id="829b6-170">其他资源</span><span class="sxs-lookup"><span data-stu-id="829b6-170">Additional resources</span></span>

* <xref:mvc/compatibility-version>
* <xref:fundamentals/metapackage-app>
* [<span data-ttu-id="829b6-171">隐式包引用</span><span class="sxs-lookup"><span data-stu-id="829b6-171">Implicit package references</span></span>](/dotnet/core/tools/csproj#implicit-package-references)
