---
title: 从 ASP.NET Core 迁移 2.2 到 3.0 预览
author: tdykstra
description: 了解如何将 ASP.NET Core 2.2 项目迁移到 ASP.NET Core 3.0。
ms.author: tdykstra
ms.custom: mvc
ms.date: 06/17/2019
uid: migration/22-to-30
ms.openlocfilehash: 13108732042244764d5c134771f701030630a260
ms.sourcegitcommit: 040aedca220ed24ee1726e6886daf6906f95a028
ms.translationtype: MT
ms.contentlocale: zh-CN
ms.lasthandoff: 07/15/2019
ms.locfileid: "67892884"
---
# <a name="migrate-from-aspnet-core-22-to-30"></a><span data-ttu-id="e676a-103">从 ASP.NET Core 2.2 到 3.0 迁移</span><span class="sxs-lookup"><span data-stu-id="e676a-103">Migrate from ASP.NET Core 2.2 to 3.0</span></span>

<span data-ttu-id="e676a-104">通过[Scott Addie](https://github.com/scottaddie)和[Rick Anderson](https://twitter.com/RickAndMSFT)</span><span class="sxs-lookup"><span data-stu-id="e676a-104">By [Scott Addie](https://github.com/scottaddie) and [Rick Anderson](https://twitter.com/RickAndMSFT)</span></span>

<span data-ttu-id="e676a-105">本文介绍如何更新现有的 ASP.NET Core 2.2 项目到 ASP.NET Core 3.0。</span><span class="sxs-lookup"><span data-stu-id="e676a-105">This article explains how to update an existing ASP.NET Core 2.2 project to ASP.NET Core 3.0.</span></span>

## <a name="prerequisites"></a><span data-ttu-id="e676a-106">系统必备</span><span class="sxs-lookup"><span data-stu-id="e676a-106">Prerequisites</span></span>

# <a name="visual-studiotabvisual-studio"></a>[<span data-ttu-id="e676a-107">Visual Studio</span><span class="sxs-lookup"><span data-stu-id="e676a-107">Visual Studio</span></span>](#tab/visual-studio)

[!INCLUDE[](~/includes/net-core-prereqs-vs-3.0.md)]

# <a name="visual-studio-codetabvisual-studio-code"></a>[<span data-ttu-id="e676a-108">Visual Studio Code</span><span class="sxs-lookup"><span data-stu-id="e676a-108">Visual Studio Code</span></span>](#tab/visual-studio-code)

[!INCLUDE[](~/includes/net-core-prereqs-vsc-3.0.md)]

# <a name="visual-studio-for-mactabvisual-studio-mac"></a>[<span data-ttu-id="e676a-109">Visual Studio for Mac</span><span class="sxs-lookup"><span data-stu-id="e676a-109">Visual Studio for Mac</span></span>](#tab/visual-studio-mac)

[!INCLUDE[](~/includes/net-core-prereqs-mac-3.0.md)]

---

## <a name="update-the-project-file"></a><span data-ttu-id="e676a-110">更新项目文件</span><span class="sxs-lookup"><span data-stu-id="e676a-110">Update the project file</span></span>

* <span data-ttu-id="e676a-111">设置[目标框架名字对象 (TFM)](/dotnet/standard/frameworks)到`netcoreapp3.0`:</span><span class="sxs-lookup"><span data-stu-id="e676a-111">Set the [Target Framework Moniker (TFM)](/dotnet/standard/frameworks) to `netcoreapp3.0`:</span></span>

  ```xml
  <TargetFramework>netcoreapp3.0</TargetFramework>
  ```

* <span data-ttu-id="e676a-112">删除任何`<PackageReference>`到[Microsoft.AspNetCore.All](xref:fundamentals/metapackage)或[Microsoft.AspNetCore.App](xref:fundamentals/metapackage-app)元包。</span><span class="sxs-lookup"><span data-stu-id="e676a-112">Remove any `<PackageReference>` to the [Microsoft.AspNetCore.All](xref:fundamentals/metapackage) or [Microsoft.AspNetCore.App](xref:fundamentals/metapackage-app) metapackage.</span></span>

* <span data-ttu-id="e676a-113">更新`Version`特性上剩余`<PackageReference>`元素`Microsoft.AspNetCore.*`程序包添加到当前的预览版中 (例如， `3.0.0-preview5-19227-01`)。</span><span class="sxs-lookup"><span data-stu-id="e676a-113">Update the `Version` attribute on remaining `<PackageReference>` elements for `Microsoft.AspNetCore.*` packages to the current preview (for example, `3.0.0-preview5-19227-01`).</span></span>

  <span data-ttu-id="e676a-114">如果没有版本 3.0 的包，包可能会弃用在 3.0 中。</span><span class="sxs-lookup"><span data-stu-id="e676a-114">If there's no 3.0 version of a package, the package might have been deprecated in 3.0.</span></span> <span data-ttu-id="e676a-115">许多这些包属于`Microsoft.AspNetCore.App`，并且不应单独引用。</span><span class="sxs-lookup"><span data-stu-id="e676a-115">Many of these packages are part of `Microsoft.AspNetCore.App` and shouldn't be referenced individually.</span></span> <span data-ttu-id="e676a-116">初步不再生成在 3.0 中的包的列表，请参阅[停止在 3.0 中的共享的框架程序集生成包 (aspnet/AspNetCore #3756)](https://github.com/aspnet/AspNetCore/issues/3756)。</span><span class="sxs-lookup"><span data-stu-id="e676a-116">For a preliminary list of packages no longer produced in 3.0, see [Stop producing packages for shared framework assemblies in 3.0 (aspnet/AspNetCore #3756)](https://github.com/aspnet/AspNetCore/issues/3756).</span></span> <span data-ttu-id="e676a-117">*共享的框架*是程序集的集合 ( *.dll*文件)，是在计算机上安装并引用`Microsoft.AspNetCore.App`。</span><span class="sxs-lookup"><span data-stu-id="e676a-117">The *shared framework* is the set of assemblies (*.dll* files) that are installed on the machine and referenced by `Microsoft.AspNetCore.App`.</span></span> <span data-ttu-id="e676a-118">有关详细信息，请参阅[共享框架](https://natemcmaster.com/blog/2018/08/29/netcore-primitives-2/)。</span><span class="sxs-lookup"><span data-stu-id="e676a-118">For more information, see [The shared framework](https://natemcmaster.com/blog/2018/08/29/netcore-primitives-2/).</span></span>

* <span data-ttu-id="e676a-119">值得注意的几个组件的程序集已从`Microsoft.AspNetCore.App`在 3.0 中。</span><span class="sxs-lookup"><span data-stu-id="e676a-119">The assemblies for several notable components were removed from `Microsoft.AspNetCore.App` in 3.0.</span></span> <span data-ttu-id="e676a-120">添加`<PackageReference>`如果你使用的 Api 中列出的包中的元素[正在从 Microsoft.AspNetCore.App 3.0 中删除的程序集 (aspnet/AspNetCore #3755)](https://github.com/aspnet/AspNetCore/issues/3755)。</span><span class="sxs-lookup"><span data-stu-id="e676a-120">Add `<PackageReference>` elements if you're using APIs from packages listed in [Assemblies being removed from Microsoft.AspNetCore.App 3.0 (aspnet/AspNetCore #3755)](https://github.com/aspnet/AspNetCore/issues/3755).</span></span>

  <span data-ttu-id="e676a-121">已删除组件的示例包括：</span><span class="sxs-lookup"><span data-stu-id="e676a-121">Examples of removed components include:</span></span>

  * `Microsoft.AspNet.WebApi.Client`
  * `Microsoft.EntityFrameworkCore`
  * `System.Data.SqlClient`

  <span data-ttu-id="e676a-122">在进行发布的程序集列表`Microsoft.AspNetCore.App`尚未完成，并且在 3.0 RTM 之前会更改。</span><span class="sxs-lookup"><span data-stu-id="e676a-122">The list of assemblies shipping in `Microsoft.AspNetCore.App` hasn't been finalized and will change before 3.0 RTM.</span></span>

  <span data-ttu-id="e676a-123">考虑下列代码：</span><span class="sxs-lookup"><span data-stu-id="e676a-123">Consider the following code:</span></span>

  ```csharp
  var branches = await response.Content.ReadAsAsync<IEnumerable<GitHubBranch>>();
  ```

  <span data-ttu-id="e676a-124">`ReadAsAsync`中包含在前面的代码中调用的方法`Microsoft.AspNet.WebApi.Client`。</span><span class="sxs-lookup"><span data-stu-id="e676a-124">The `ReadAsAsync` method called in the preceding code is included in `Microsoft.AspNet.WebApi.Client`.</span></span> <span data-ttu-id="e676a-125">安装[Microsoft.AspNet.WebApi.Client](https://www.nuget.org/packages/Microsoft.AspNet.WebApi.Client/) NuGet 程序包来解决 3.0 中的编译问题。</span><span class="sxs-lookup"><span data-stu-id="e676a-125">Install the [Microsoft.AspNet.WebApi.Client](https://www.nuget.org/packages/Microsoft.AspNet.WebApi.Client/) NuGet package to resolve the compilation issue in 3.0.</span></span>

* <span data-ttu-id="e676a-126">添加[Json.NET 支持](#jsonnet-support)。</span><span class="sxs-lookup"><span data-stu-id="e676a-126">Add [Json.NET support](#jsonnet-support).</span></span>

* <span data-ttu-id="e676a-127">项目默认为[进程内承载模型](xref:host-and-deploy/aspnet-core-module#in-process-hosting-model)在 ASP.NET Core 3.0 或更高版本。</span><span class="sxs-lookup"><span data-stu-id="e676a-127">Projects default to the [in-process hosting model](xref:host-and-deploy/aspnet-core-module#in-process-hosting-model) in ASP.NET Core 3.0 or later.</span></span> <span data-ttu-id="e676a-128">您还可以删除`<AspNetCoreHostingModel>`属性中的项目文件，如果其值为`InProcess`。</span><span class="sxs-lookup"><span data-stu-id="e676a-128">You may optionally remove the `<AspNetCoreHostingModel>` property in the project file if its value is `InProcess`.</span></span>

## <a name="jsonnet-support"></a><span data-ttu-id="e676a-129">Json.NET 支持</span><span class="sxs-lookup"><span data-stu-id="e676a-129">Json.NET support</span></span>

<span data-ttu-id="e676a-130">为工作的一部分[提高 ASP.NET Core 共享的框架](https://blogs.msdn.microsoft.com/webdev/2018/10/29/a-first-look-at-changes-coming-in-asp-net-core-3-0/)， [Json.NET](https://www.newtonsoft.com/json/help/html/Introduction.htm)已从 ASP.NET Core 共享框架中删除。</span><span class="sxs-lookup"><span data-stu-id="e676a-130">As part of the work to [improve the ASP.NET Core shared framework](https://blogs.msdn.microsoft.com/webdev/2018/10/29/a-first-look-at-changes-coming-in-asp-net-core-3-0/), [Json.NET](https://www.newtonsoft.com/json/help/html/Introduction.htm) has been removed from the ASP.NET Core shared framework.</span></span>

<span data-ttu-id="e676a-131">若要在 ASP.NET Core 3.0 项目中使用 Json.NET:</span><span class="sxs-lookup"><span data-stu-id="e676a-131">To use Json.NET in an ASP.NET Core 3.0 project:</span></span>

* <span data-ttu-id="e676a-132">添加到包引用[Microsoft.AspNetCore.Mvc.NewtonsoftJson](https://nuget.org/packages/Microsoft.AspNetCore.Mvc.NewtonsoftJson)。</span><span class="sxs-lookup"><span data-stu-id="e676a-132">Add a package reference to [Microsoft.AspNetCore.Mvc.NewtonsoftJson](https://nuget.org/packages/Microsoft.AspNetCore.Mvc.NewtonsoftJson).</span></span>
* <span data-ttu-id="e676a-133">更新`Startup.ConfigureServices`调用`AddNewtonsoftJson`。</span><span class="sxs-lookup"><span data-stu-id="e676a-133">Update `Startup.ConfigureServices` to call `AddNewtonsoftJson`.</span></span>

  ```csharp
  services.AddMvc()
      .AddNewtonsoftJson();
  ```
  
  <span data-ttu-id="e676a-134">`AddNewtonsoftJson` 适用于新的 MVC 服务注册方法：</span><span class="sxs-lookup"><span data-stu-id="e676a-134">`AddNewtonsoftJson` is compatible with the new MVC service registration methods:</span></span>

  * `AddRazorPages`
  * `AddControllersWithViews`
  * `AddControllers`

  ```csharp
  services.AddControllers()
      .AddNewtonsoftJson();
  ```

  <span data-ttu-id="e676a-135">对的调用中，可以设置 Json.NET 设置`AddNewtonsoftJson`:</span><span class="sxs-lookup"><span data-stu-id="e676a-135">Json.NET settings can be set in the call to `AddNewtonsoftJson`:</span></span>

  ```csharp
  services.AddMvc()
      .AddNewtonsoftJson(options =>
             options.SerializerSettings.ContractResolver =
                new CamelCasePropertyNamesContractResolver());
  ```

## <a name="mvc-service-registration"></a><span data-ttu-id="e676a-136">MVC 服务注册</span><span class="sxs-lookup"><span data-stu-id="e676a-136">MVC service registration</span></span>

<span data-ttu-id="e676a-137">ASP.NET Core 3.0 添加了用于注册内部的 MVC 方案的新选项`Startup.ConfigureServices`。</span><span class="sxs-lookup"><span data-stu-id="e676a-137">ASP.NET Core 3.0 adds new options for registering MVC scenarios inside `Startup.ConfigureServices`.</span></span>

<span data-ttu-id="e676a-138">三个新的顶级扩展方法与 MVC 方案上`IServiceCollection`可用。</span><span class="sxs-lookup"><span data-stu-id="e676a-138">Three new top-level extension methods related to MVC scenarios on `IServiceCollection` are available.</span></span> <span data-ttu-id="e676a-139">模板使用这些新方法，而不是`UseMvc`。</span><span class="sxs-lookup"><span data-stu-id="e676a-139">Templates use these new methods instead of `UseMvc`.</span></span> <span data-ttu-id="e676a-140">但是，`AddMvc`继续像它已在以前的版本。</span><span class="sxs-lookup"><span data-stu-id="e676a-140">However, `AddMvc` continues to behave as it has in previous releases.</span></span>

<span data-ttu-id="e676a-141">下面的示例将添加对控制器和与 API 相关的功能，但不是视图或页面的支持。</span><span class="sxs-lookup"><span data-stu-id="e676a-141">The following example adds support for controllers and API-related features, but not views or pages.</span></span> <span data-ttu-id="e676a-142">API 模板使用此代码：</span><span class="sxs-lookup"><span data-stu-id="e676a-142">The API template uses this code:</span></span>

```csharp
public void ConfigureServices(IServiceCollection services)
{
    services.AddControllers();
}
```

<span data-ttu-id="e676a-143">下面的示例将添加对控制器、 与 API 相关的功能，和视图，但不是页面的支持。</span><span class="sxs-lookup"><span data-stu-id="e676a-143">The following example adds support for controllers, API-related features, and views, but not pages.</span></span> <span data-ttu-id="e676a-144">Web 应用程序 (MVC) 模板使用此代码：</span><span class="sxs-lookup"><span data-stu-id="e676a-144">The Web Application (MVC) template uses this code:</span></span>

```csharp
public void ConfigureServices(IServiceCollection services)
{
    services.AddControllersWithViews();
}
```

<span data-ttu-id="e676a-145">下面的示例添加支持 Razor 页面和最小控制器支持。</span><span class="sxs-lookup"><span data-stu-id="e676a-145">The following example adds support for Razor Pages and minimal controller support.</span></span> <span data-ttu-id="e676a-146">Web 应用程序模板使用此代码：</span><span class="sxs-lookup"><span data-stu-id="e676a-146">The Web Application template uses this code:</span></span>

```csharp
public void ConfigureServices(IServiceCollection services)
{
    services.AddRazorPages();
}
```

<span data-ttu-id="e676a-147">此外可以组合的新方法。</span><span class="sxs-lookup"><span data-stu-id="e676a-147">The new methods can also be combined.</span></span> <span data-ttu-id="e676a-148">下面的示例是等效于调用`AddMvc`ASP.NET Core 2.2 中：</span><span class="sxs-lookup"><span data-stu-id="e676a-148">The following example is equivalent to calling `AddMvc` in ASP.NET Core 2.2:</span></span> 

```csharp
public void ConfigureServices(IServiceCollection services)
{
    services.AddControllers();
    services.AddRazorPages();
}
```

## <a name="update-routing-startup-code"></a><span data-ttu-id="e676a-149">更新路由启动代码</span><span class="sxs-lookup"><span data-stu-id="e676a-149">Update routing startup code</span></span>

<span data-ttu-id="e676a-150">如果应用程序调用`UseMvc`或`UseSignalR`，将迁移到应用程序[终结点路由](xref:fundamentals/routing)在可能的情况。</span><span class="sxs-lookup"><span data-stu-id="e676a-150">If an app calls `UseMvc` or `UseSignalR`, migrate the app to [Endpoint Routing](xref:fundamentals/routing) if possible.</span></span> <span data-ttu-id="e676a-151">若要提高终结点路由与 MVC 的以前版本的兼容性，我们已还原的一些变化中引入了 ASP.NET Core 2.2 中的 URL 生成。</span><span class="sxs-lookup"><span data-stu-id="e676a-151">To improve Endpoint Routing compatibility with previous versions of MVC, we've reverted some of the changes in URL generation introduced in ASP.NET Core 2.2.</span></span> <span data-ttu-id="e676a-152">如果您遇到 2.2 中使用终结点路由时出现问题，但存在以下例外在 ASP.NET Core 3.0 中预期的改进：</span><span class="sxs-lookup"><span data-stu-id="e676a-152">If you experienced problems using Endpoint Routing in 2.2, expect improvements in ASP.NET Core 3.0 with the following exceptions:</span></span>

* <span data-ttu-id="e676a-153">如果应用实施`IRouter`或继承自`Route`，可能想要避免这一次迁移。</span><span class="sxs-lookup"><span data-stu-id="e676a-153">If the app implements `IRouter` or inherits from `Route`, you may want to avoid migrating at this time.</span></span> <span data-ttu-id="e676a-154">提供在反馈[打算迁移到终结点路由基于 IRouter 实现](https://github.com/aspnet/AspNetCore/issues/4221)。</span><span class="sxs-lookup"><span data-stu-id="e676a-154">Provide feedback at [Plan to migrate IRouter based implementations onto Endpoint Routing](https://github.com/aspnet/AspNetCore/issues/4221).</span></span>

* <span data-ttu-id="e676a-155">如果应用程序直接访问`RouteData.Routers`在 MVC 中，你可能想要避免这一次迁移。</span><span class="sxs-lookup"><span data-stu-id="e676a-155">If the app directly accesses `RouteData.Routers` inside MVC, you may want to avoid migrating at this time.</span></span> <span data-ttu-id="e676a-156">提供在反馈[迁移指南为使用 RouteData.Routers](https://github.com/aspnet/AspNetCore/issues/9148)。</span><span class="sxs-lookup"><span data-stu-id="e676a-156">Provide feedback at [Migration guidance for using RouteData.Routers](https://github.com/aspnet/AspNetCore/issues/9148).</span></span>

<span data-ttu-id="e676a-157">终结点路由支持相同的路由模式语法和创作功能作为路由模式`IRouter`。</span><span class="sxs-lookup"><span data-stu-id="e676a-157">Endpoint Routing supports the same route pattern syntax and route pattern authoring features as `IRouter`.</span></span> <span data-ttu-id="e676a-158">终结点路由支持`IRouteContraint`。</span><span class="sxs-lookup"><span data-stu-id="e676a-158">Endpoint Routing supports `IRouteContraint`.</span></span> <span data-ttu-id="e676a-159">终结点路由支持`[Route]`， `[HttpGet]`，和其他 MVC 路由属性。</span><span class="sxs-lookup"><span data-stu-id="e676a-159">Endpoint routing supports `[Route]`, `[HttpGet]`, and the other MVC routing attributes.</span></span>

<span data-ttu-id="e676a-160">对于大多数应用程序，仅`Startup`还需要进行更改。</span><span class="sxs-lookup"><span data-stu-id="e676a-160">For most applications, only `Startup` requires changes.</span></span>

### <a name="migrate-startupconfigure"></a><span data-ttu-id="e676a-161">迁移 Startup.Configure</span><span class="sxs-lookup"><span data-stu-id="e676a-161">Migrate Startup.Configure</span></span>

<span data-ttu-id="e676a-162">一般不建议：</span><span class="sxs-lookup"><span data-stu-id="e676a-162">General advice:</span></span>

* <span data-ttu-id="e676a-163">添加`UseRouting`。</span><span class="sxs-lookup"><span data-stu-id="e676a-163">Add `UseRouting`.</span></span> 
* <span data-ttu-id="e676a-164">如果该应用程序调用`UseStaticFiles`，将置于`UseStaticFiles`**之前** `UseRouting`。</span><span class="sxs-lookup"><span data-stu-id="e676a-164">If the app calls `UseStaticFiles`, place `UseStaticFiles` **before** `UseRouting`.</span></span>
* <span data-ttu-id="e676a-165">如果应用使用身份验证/授权功能，如`AuthorizePage`或`[Authorize]`，将对`UseAuthentication`并`UseAuthorization`**后** `UseRouting`。</span><span class="sxs-lookup"><span data-stu-id="e676a-165">If the app uses authentication/authorization features such as `AuthorizePage` or `[Authorize]`, place the call to `UseAuthentication` and `UseAuthorization` **after** `UseRouting`.</span></span>
* <span data-ttu-id="e676a-166">如果应用使用[CORS](xref:security/cors)功能，如`[EnableCors]`，将放置`UseCors`下一步。</span><span class="sxs-lookup"><span data-stu-id="e676a-166">If the app uses [CORS](xref:security/cors) features, such as `[EnableCors]`, place `UseCors` next.</span></span>
* <span data-ttu-id="e676a-167">替换`UseMvc`或`UseSignalR`与`UseEndpoints`。</span><span class="sxs-lookup"><span data-stu-id="e676a-167">Replace `UseMvc` or `UseSignalR` with `UseEndpoints`.</span></span>

<span data-ttu-id="e676a-168">以下是一种`Startup.Configure`典型的 ASP.NET Core 2.2 应用中：</span><span class="sxs-lookup"><span data-stu-id="e676a-168">The following is an example of `Startup.Configure` in a typical ASP.NET Core 2.2 app:</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseStaticFiles();
    
    app.UseAuthentication();

    app.UseSignalR(hubs =>
    {
        hubs.MapHub<ChatHub>("/chat");
    });

    app.UseMvc(routes =>
    {
        routes.MapRoute("default", "{controller=Home}/{action=Index}/{id?}");
    });
}
```

<span data-ttu-id="e676a-169">更新以前后`Startup.Configure`代码：</span><span class="sxs-lookup"><span data-stu-id="e676a-169">After updating the previous `Startup.Configure` code:</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseStaticFiles();
    
    app.UseRouting();

    app.UseAuthentication();
    app.UseAuthorization();
    app.UseCors();

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapHub<ChatHub>("/chat");
        endpoints.MapControllerRoute("default", "{controller=Home}/{action=Index}/{id?}");
    });
}
```

### <a name="security-middleware-guidance"></a><span data-ttu-id="e676a-170">安全中间件指南</span><span class="sxs-lookup"><span data-stu-id="e676a-170">Security middleware guidance</span></span>

<span data-ttu-id="e676a-171">对授权和 CORS 的支持围绕统一[中间件](xref:fundamentals/middleware/index)方法。</span><span class="sxs-lookup"><span data-stu-id="e676a-171">Support for authorization and CORS is unified around the [middleware](xref:fundamentals/middleware/index) approach.</span></span> <span data-ttu-id="e676a-172">这样在这些方案的同一个中间件和功能。</span><span class="sxs-lookup"><span data-stu-id="e676a-172">This allows use of the same middleware and functionality across these scenarios.</span></span> <span data-ttu-id="e676a-173">在此版本中，提供已更新的授权中间件，CORS 中间件得到了增强，以便它可以理解使用 MVC 控制器的属性。</span><span class="sxs-lookup"><span data-stu-id="e676a-173">An updated authorization middleware is provided in this release, and CORS Middleware is enhanced so that it can understand the attributes used by MVC controllers.</span></span>

#### <a name="cors"></a><span data-ttu-id="e676a-174">CORS</span><span class="sxs-lookup"><span data-stu-id="e676a-174">CORS</span></span>

<span data-ttu-id="e676a-175">以前，CORS 可能很难配置。</span><span class="sxs-lookup"><span data-stu-id="e676a-175">Previously, CORS could be difficult to configure.</span></span> <span data-ttu-id="e676a-176">对于使用在某些用例，但 MVC 筛选器应使用提供的中间件**而无需**在其他中间件的用例。</span><span class="sxs-lookup"><span data-stu-id="e676a-176">Middleware was provided for use in some use cases, but MVC filters were intended to be used **without** the middleware in other use cases.</span></span> <span data-ttu-id="e676a-177">使用 ASP.NET Core 3.0 中，我们建议需要 CORS 的所有应用中与终结点路由协同都使用 CORS 中间件。</span><span class="sxs-lookup"><span data-stu-id="e676a-177">With ASP.NET Core 3.0, we recommend that all apps that require CORS use the CORS Middleware in tandem with Endpoint Routing.</span></span> <span data-ttu-id="e676a-178">`UseCors` 可以使用默认策略，提供并`[EnableCors]`和`[DisableCors]`属性可用于覆盖默认策略需要。</span><span class="sxs-lookup"><span data-stu-id="e676a-178">`UseCors` can be provided with a default policy, and `[EnableCors]` and `[DisableCors]` attributes can be used to override the default policy where required.</span></span> 

<span data-ttu-id="e676a-179">如下示例中：</span><span class="sxs-lookup"><span data-stu-id="e676a-179">In the following example:</span></span>

* <span data-ttu-id="e676a-180">为使用的所有终结点启用了 CORS`default`命名策略。</span><span class="sxs-lookup"><span data-stu-id="e676a-180">CORS is enabled for all endpoints with the `default` named policy.</span></span>
* <span data-ttu-id="e676a-181">`MyController`类禁用 CORS 的`[DisableCors]`属性。</span><span class="sxs-lookup"><span data-stu-id="e676a-181">The `MyController` class disables CORS with the `[DisableCors]` attribute.</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseCors("default"); 

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapDefaultControllerRoute();
    });
}

[DisableCors]
public class MyController : ControllerBase
{
    ...
}
```

#### <a name="authorization"></a><span data-ttu-id="e676a-182">授权</span><span class="sxs-lookup"><span data-stu-id="e676a-182">Authorization</span></span>

<span data-ttu-id="e676a-183">在早期版本的 ASP.NET Core，身份验证支持通过提供`[Authorize]`属性。</span><span class="sxs-lookup"><span data-stu-id="e676a-183">In earlier versions of ASP.NET Core, authorization support was provided via the `[Authorize]` attribute.</span></span> <span data-ttu-id="e676a-184">授权中间件不可用。</span><span class="sxs-lookup"><span data-stu-id="e676a-184">Authorization middleware wasn't available.</span></span> <span data-ttu-id="e676a-185">在 ASP.NET Core 3.0 中，授权中间件是必需的。</span><span class="sxs-lookup"><span data-stu-id="e676a-185">In ASP.NET Core 3.0, authorization middleware is required.</span></span> <span data-ttu-id="e676a-186">建议您将 ASP.NET Core 授权中间件 (`UseAuthorization`) 后立即`UseAuthentication`。</span><span class="sxs-lookup"><span data-stu-id="e676a-186">We recommend placing the ASP.NET Core Authorization Middleware (`UseAuthorization`) immediately after `UseAuthentication`.</span></span> <span data-ttu-id="e676a-187">此外可以使用默认策略，可以重写配置授权中间件。</span><span class="sxs-lookup"><span data-stu-id="e676a-187">The Authorization Middleware can also be configured with a default policy, which can be overridden.</span></span>

<span data-ttu-id="e676a-188">在 ASP.NET Core 3.0 或更高版本，`UseAuthorization`中称为`Startup.Configure`，和以下`HomeController`中用户需要一个已签名：</span><span class="sxs-lookup"><span data-stu-id="e676a-188">In ASP.NET Core 3.0 or later, `UseAuthorization` is called in `Startup.Configure`, and the following `HomeController` requires a signed in user:</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseAuthentication();
    app.UseAuthorization();

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapDefaultControllerRoute();
    });
}

public class HomeController : ControllerBase
{
    [Authorize]
    public IActionResult BuyWidgets()
    {
        ...
    }
}
```

<span data-ttu-id="e676a-189">如果应用使用`AuthorizeFilter`作为全局筛选器在 MVC 中，我们建议提供一个策略，以将代码重构`UseAuthorization`中间件。</span><span class="sxs-lookup"><span data-stu-id="e676a-189">If the app uses an `AuthorizeFilter` as a global filter in MVC, we recommend refactoring the code to provide a policy to the `UseAuthorization` middleware.</span></span>

<span data-ttu-id="e676a-190">在以下示例中，若要应用于所有的自定义策略请求时`UseAuthorization`调用时，和`HomeController`而无需登录到应用的用户允许访问：</span><span class="sxs-lookup"><span data-stu-id="e676a-190">In the following example, a custom policy to be applied to all requests when `UseAuthorization` is called, and the `HomeController` allows access without the user signing into the app:</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseAuthentication();
    app.UseAuthorization(new AuthorizationPolicyBuilder().Build()));

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapDefaultControllerRoute();
    });
}

[AllowAnonymous]
public class HomeController : ControllerBase
{
    ...
}
```

<span data-ttu-id="e676a-191">授权由中间件的工作而无需具有授权的任何特定的知识的 framework。</span><span class="sxs-lookup"><span data-stu-id="e676a-191">Authorization by middleware works without the framework having any specific knowledge of authorization.</span></span> <span data-ttu-id="e676a-192">例如，[运行状况检查](xref:host-and-deploy/health-checks)一无所知特定的授权，但运行状况检查都可以由中间件应用可配置授权策略。</span><span class="sxs-lookup"><span data-stu-id="e676a-192">For instance, [health checks](xref:host-and-deploy/health-checks) has no specific knowledge of authorization, but health checks can have a configurable authorization policy applied by the middleware.</span></span>

<span data-ttu-id="e676a-193">在以下示例中，`UseAuthorization`处理授权不使用默认策略，但`/healthz`运行状况检查终结点要求用户在`admin`角色：</span><span class="sxs-lookup"><span data-stu-id="e676a-193">In the following example, `UseAuthorization` processes authorization without a default policy, but the `/healthz` health check endpoint requires the user to be in the `admin` role:</span></span> 

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseAuthentication();
    app.UseAuthorization();

    app.UseEndpoints(endpoints =>
    {
        endpoints
            .MapHealthChecks("/healthz")
            .RequireAuthorization(new AuthorizeAttribute(){ Roles = "admin", });
    });
}
```

<span data-ttu-id="e676a-194">某些情况下实现保护。</span><span class="sxs-lookup"><span data-stu-id="e676a-194">Protection is implemented for some scenarios.</span></span> <span data-ttu-id="e676a-195">`UseEndpoint` 如果授权或 CORS 策略由于将跳过缺少中间件，中间件引发异常。</span><span class="sxs-lookup"><span data-stu-id="e676a-195">`UseEndpoint` middleware throws an exception if an authorization or CORS policy is skipped due to missing middleware.</span></span> <span data-ttu-id="e676a-196">分析器支持可提供其他反馈配置错误正在进行中。</span><span class="sxs-lookup"><span data-stu-id="e676a-196">Analyzer support to provide additional feedback about misconfiguration is in progress.</span></span>

### <a name="migrate-signalr"></a><span data-ttu-id="e676a-197">迁移 SignalR</span><span class="sxs-lookup"><span data-stu-id="e676a-197">Migrate SignalR</span></span>

<span data-ttu-id="e676a-198">现在的 SignalR 集线器映射内发生`UseEndpoints`。</span><span class="sxs-lookup"><span data-stu-id="e676a-198">Mapping of SignalR hubs now takes place inside `UseEndpoints`.</span></span> 

<span data-ttu-id="e676a-199">将映射与每个中心`MapHub`。</span><span class="sxs-lookup"><span data-stu-id="e676a-199">Map each hub with `MapHub`.</span></span> <span data-ttu-id="e676a-200">在上一版本中，明确列出每个中心。</span><span class="sxs-lookup"><span data-stu-id="e676a-200">As in previous versions, each hub is explicitly listed.</span></span>

<span data-ttu-id="e676a-201">在以下示例中，支持`ChatHub`SignalR 集线器添加：</span><span class="sxs-lookup"><span data-stu-id="e676a-201">In the following example, support for the `ChatHub` SignalR hub is added:</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapHub<ChatHub>();
    });
}
```

### <a name="migrate-mvc-controllers"></a><span data-ttu-id="e676a-202">迁移 MVC 控制器</span><span class="sxs-lookup"><span data-stu-id="e676a-202">Migrate MVC controllers</span></span>

<span data-ttu-id="e676a-203">现在的控制器映射内发生`UseEndpoints`。</span><span class="sxs-lookup"><span data-stu-id="e676a-203">Mapping of controllers now takes place inside `UseEndpoints`.</span></span> 

<span data-ttu-id="e676a-204">添加`MapControllers`如果应用使用属性路由。</span><span class="sxs-lookup"><span data-stu-id="e676a-204">Add `MapControllers` if the app uses attribute routing.</span></span> <span data-ttu-id="e676a-205">由于路由包括对许多框架在 ASP.NET Core 3.0 或更高版本的支持，添加属性路由的控制器是参加。</span><span class="sxs-lookup"><span data-stu-id="e676a-205">Since routing includes support for many frameworks in ASP.NET Core 3.0 or later, adding attribute-routed controllers is opt-in.</span></span> 

<span data-ttu-id="e676a-206">将为以下内容：</span><span class="sxs-lookup"><span data-stu-id="e676a-206">Replace the following:</span></span>

* <span data-ttu-id="e676a-207">`MapRoute` 使用 `MapControllerRoute`</span><span class="sxs-lookup"><span data-stu-id="e676a-207">`MapRoute` with `MapControllerRoute`</span></span>
* <span data-ttu-id="e676a-208">`MapAreaRoute` 使用 `MapAreaControllerRoute`</span><span class="sxs-lookup"><span data-stu-id="e676a-208">`MapAreaRoute` with `MapAreaControllerRoute`</span></span>

<span data-ttu-id="e676a-209">由于路由现在包括对不止是 MVC 的支持，已更改了术语进行明确说明他们所做的这些方法。</span><span class="sxs-lookup"><span data-stu-id="e676a-209">Since routing now includes support for more than just MVC, the terminology has changed to make these methods clearly state what they do.</span></span> <span data-ttu-id="e676a-210">如传统路由`MapControllerRoute` / `MapAreaControllerRoute` / `MapDefaultControllerRoute`它们要添加的顺序应用。</span><span class="sxs-lookup"><span data-stu-id="e676a-210">Conventional routes such as `MapControllerRoute`/`MapAreaControllerRoute`/`MapDefaultControllerRoute` are applied in the order that they're added.</span></span> <span data-ttu-id="e676a-211">将第一位更具体的路由 （如某一区域的路由）。</span><span class="sxs-lookup"><span data-stu-id="e676a-211">Place more specific routes (such as routes for an area) first.</span></span>

<span data-ttu-id="e676a-212">如下示例中：</span><span class="sxs-lookup"><span data-stu-id="e676a-212">In the following example:</span></span>

* <span data-ttu-id="e676a-213">`MapControllers` 添加了对属性路由的控制器支持。</span><span class="sxs-lookup"><span data-stu-id="e676a-213">`MapControllers` adds support for attribute-routed controllers.</span></span>
* <span data-ttu-id="e676a-214">`MapAreaControllerRoute` 将控制器的传统路由添加区域。</span><span class="sxs-lookup"><span data-stu-id="e676a-214">`MapAreaControllerRoute` adds a conventional route for controllers in an area.</span></span>
* <span data-ttu-id="e676a-215">`MapControllerRoute` 添加控制器的常规路由。</span><span class="sxs-lookup"><span data-stu-id="e676a-215">`MapControllerRoute` adds a conventional route for controllers.</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapControllers();
        endpoints.MapAreaControllerRoute(
            "admin", 
            "admin", 
            "Admin/{controller=Home}/{action=Index}/{id?}");
        endpoints.MapControllerRoute(
            "default", "{controller=Home}/{action=Index}/{id?}");
    });
}
```

### <a name="migrate-razor-pages"></a><span data-ttu-id="e676a-216">迁移 Razor 页面</span><span class="sxs-lookup"><span data-stu-id="e676a-216">Migrate Razor Pages</span></span>

<span data-ttu-id="e676a-217">现在映射 Razor 页面内发生`UseEndpoints`。</span><span class="sxs-lookup"><span data-stu-id="e676a-217">Mapping Razor Pages now takes place inside `UseEndpoints`.</span></span>

<span data-ttu-id="e676a-218">添加`MapRazorPages`如果应用使用 Razor 页面。</span><span class="sxs-lookup"><span data-stu-id="e676a-218">Add `MapRazorPages` if the app uses Razor Pages.</span></span> <span data-ttu-id="e676a-219">由于终结点路由包括对许多框架的支持添加 Razor 页面现在参加。</span><span class="sxs-lookup"><span data-stu-id="e676a-219">Since Endpoint Routing includes support for many frameworks, adding Razor Pages is now opt-in.</span></span>

<span data-ttu-id="e676a-220">在以下示例中，`MapRazorPages`添加了对 Razor 页面支持：</span><span class="sxs-lookup"><span data-stu-id="e676a-220">In the following example, `MapRazorPages` adds support for Razor Pages:</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapRazorPages();
    });
}
```

### <a name="use-mvc-without-endpoint-routing"></a><span data-ttu-id="e676a-221">使用 MVC 而无需终结点路由</span><span class="sxs-lookup"><span data-stu-id="e676a-221">Use MVC without Endpoint Routing</span></span>

<span data-ttu-id="e676a-222">使用通过 MVC`UseMvc`或`UseMvcWithDefaultRoute`在 ASP.NET Core 3.0 需要显式参加内`Startup.ConfigureServices`。</span><span class="sxs-lookup"><span data-stu-id="e676a-222">Using MVC via `UseMvc` or `UseMvcWithDefaultRoute` in ASP.NET Core 3.0 requires an explicit opt-in inside `Startup.ConfigureServices`.</span></span> <span data-ttu-id="e676a-223">这是必需的因为 MVC 必须知道是否它可以依赖于授权和 CORS 中间件在初始化过程。</span><span class="sxs-lookup"><span data-stu-id="e676a-223">This is required because MVC must know whether it can rely on the authorization and CORS Middleware during initialization.</span></span> <span data-ttu-id="e676a-224">分析器将提供的警告是应用程序尝试使用不支持的配置。</span><span class="sxs-lookup"><span data-stu-id="e676a-224">An analyzer is provided that warns if the app attempts to use an unsupported configuration.</span></span>

<span data-ttu-id="e676a-225">如果应用需要旧版`IRouter`支持，请禁用`EnableEndpointRouting`使用以下任一方法在`Startup.ConfigureServices`:</span><span class="sxs-lookup"><span data-stu-id="e676a-225">If the app requires legacy `IRouter` support, disable `EnableEndpointRouting` using any of the following approaches in `Startup.ConfigureServices`:</span></span>

```csharp
services.AddMvc(options => options.EnableEndpointRouting = false);
```

```csharp
services.AddControllers(options => options.EnableEndpointRouting = false);
```

```csharp
services.AddControllersWithViews(options => options.EnableEndpointRouting = false);

```

```csharp
services.AddRazorPages().AddMvcOptions(options => options.EnableEndpointRouting = false);
```

### <a name="migrate-health-checks"></a><span data-ttu-id="e676a-226">迁移运行状况检查</span><span class="sxs-lookup"><span data-stu-id="e676a-226">Migrate health checks</span></span>

<span data-ttu-id="e676a-227">可以用作运行状况检查*路由器软件*使用终结点的路由。</span><span class="sxs-lookup"><span data-stu-id="e676a-227">Health checks can be used as a *router-ware* with Endpoint Routing.</span></span>

<span data-ttu-id="e676a-228">添加`MapHealthChecks`使用运行状况检查使用终结点的路由。</span><span class="sxs-lookup"><span data-stu-id="e676a-228">Add `MapHealthChecks` to use health checks with Endpoint Routing.</span></span> <span data-ttu-id="e676a-229">`MapHealthChecks`方法接受参数类似于`UseHealthChecks`。</span><span class="sxs-lookup"><span data-stu-id="e676a-229">The `MapHealthChecks` method accepts arguments similar to `UseHealthChecks`.</span></span> <span data-ttu-id="e676a-230">使用的优点`MapHealthChecks`转移`UseHealthChecks`是可以为应用授权并具有更好地匹配策略的细粒度控制。</span><span class="sxs-lookup"><span data-stu-id="e676a-230">The advantage of using `MapHealthChecks` over `UseHealthChecks` is the ability to apply authorization and to have greater fine-grained control over the matching policy.</span></span> 

<span data-ttu-id="e676a-231">在以下示例中，`MapHealthChecks`为在运行状况检查终结点调用`/healthz`:</span><span class="sxs-lookup"><span data-stu-id="e676a-231">In the following example, `MapHealthChecks` is called for a health check endpoint at `/healthz`:</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapHealthChecks("/healthz", new HealthCheckOptions() { });
    });
}
```

## <a name="hostbuilder-replaces-webhostbuilder"></a><span data-ttu-id="e676a-232">HostBuilder 替换 WebHostBuilder</span><span class="sxs-lookup"><span data-stu-id="e676a-232">HostBuilder replaces WebHostBuilder</span></span>

<span data-ttu-id="e676a-233">使用 ASP.NET Core 3.0 模板[泛型宿主](xref:fundamentals/host/generic-host)。</span><span class="sxs-lookup"><span data-stu-id="e676a-233">The ASP.NET Core 3.0 templates use [Generic Host](xref:fundamentals/host/generic-host).</span></span> <span data-ttu-id="e676a-234">使用早期版本[Web 主机](xref:fundamentals/host/web-host)。</span><span class="sxs-lookup"><span data-stu-id="e676a-234">Previous versions used [Web Host](xref:fundamentals/host/web-host).</span></span> <span data-ttu-id="e676a-235">下面的代码显示了生成 ASP.NET Core 3.0 模板`Program`类：</span><span class="sxs-lookup"><span data-stu-id="e676a-235">The following code shows the ASP.NET Core 3.0 template generated `Program` class:</span></span>

[!code-csharp[](22-to-30/samples/Program.cs?name=snippet)]

<span data-ttu-id="e676a-236">下面的代码演示模板生成 ASP.NET Core 2.2`Program`类：</span><span class="sxs-lookup"><span data-stu-id="e676a-236">The following code shows the ASP.NET Core 2.2 template-generated `Program` class:</span></span>

[!code-csharp[](22-to-30/samples/Program2.2.cs?name=snippet)]

<span data-ttu-id="e676a-237"><xref:Microsoft.AspNetCore.Hosting.IWebHostBuilder> 将保留在 3.0，是一种`webBuilder`上面的代码示例所示。</span><span class="sxs-lookup"><span data-stu-id="e676a-237"><xref:Microsoft.AspNetCore.Hosting.IWebHostBuilder> remains in 3.0 and is the type of the `webBuilder` seen in the preceding code sample.</span></span> <span data-ttu-id="e676a-238"><xref:Microsoft.AspNetCore.Hosting.WebHostBuilder> 将在未来版本中弃用并替换为`HostBuilder`。</span><span class="sxs-lookup"><span data-stu-id="e676a-238"><xref:Microsoft.AspNetCore.Hosting.WebHostBuilder> will be deprecated in a future release and replaced by `HostBuilder`.</span></span>

<span data-ttu-id="e676a-239">从最重要的更改`WebHostBuilder`到`HostBuilder`处于[依赖关系注入 (DI)](xref:fundamentals/dependency-injection)。</span><span class="sxs-lookup"><span data-stu-id="e676a-239">The most significant change from `WebHostBuilder` to `HostBuilder` is in [dependency injection (DI)](xref:fundamentals/dependency-injection).</span></span> <span data-ttu-id="e676a-240">使用时`HostBuilder`，仅可以注入<xref:Microsoft.Extensions.Configuration.IConfiguration>并<xref:Microsoft.AspNetCore.Hosting.IHostingEnvironment>到`Startup`的构造函数。</span><span class="sxs-lookup"><span data-stu-id="e676a-240">When using `HostBuilder`, you can only inject <xref:Microsoft.Extensions.Configuration.IConfiguration> and <xref:Microsoft.AspNetCore.Hosting.IHostingEnvironment> into `Startup`'s constructor.</span></span> <span data-ttu-id="e676a-241">`HostBuilder` DI 约束：</span><span class="sxs-lookup"><span data-stu-id="e676a-241">The `HostBuilder` DI constraints:</span></span>

* <span data-ttu-id="e676a-242">启用要生成一次只能 DI 容器。</span><span class="sxs-lookup"><span data-stu-id="e676a-242">Enable the DI container to be built only one time.</span></span>
* <span data-ttu-id="e676a-243">可避免像解决单一实例的多个实例生成的对象生存期问题。</span><span class="sxs-lookup"><span data-stu-id="e676a-243">Avoids the resulting object lifetime issues like resolving multiple instances of singletons.</span></span>

## <a name="update-signalr-code"></a><span data-ttu-id="e676a-244">更新 SignalR 代码</span><span class="sxs-lookup"><span data-stu-id="e676a-244">Update SignalR code</span></span>

<span data-ttu-id="e676a-245">`System.Text.Json` 现在，中心协议的客户端和服务器使用的默认值。</span><span class="sxs-lookup"><span data-stu-id="e676a-245">`System.Text.Json` is now the default Hub Protocol used by both the client and server.</span></span>

<span data-ttu-id="e676a-246">在中`Startup.ConfigureServices`，调用`AddJsonProtocol`设置序列化程序选项。</span><span class="sxs-lookup"><span data-stu-id="e676a-246">In `Startup.ConfigureServices`, call `AddJsonProtocol` to set serializer options.</span></span>

<span data-ttu-id="e676a-247">**服务器：**</span><span class="sxs-lookup"><span data-stu-id="e676a-247">**Server:**</span></span>

```csharp
services.AddSignalR(...)
        .AddJsonProtocol(options =>
        {
            options.WriteIndented = false;
        })
```

<span data-ttu-id="e676a-248">**客户端：**</span><span class="sxs-lookup"><span data-stu-id="e676a-248">**Client:**</span></span>

```csharp
new HubConnectionBuilder()
    .WithUrl("/chatHub")
    .AddJsonProtocol(options =>
    {
        options.WriteIndented = false;
    })
    .Build();
```

### <a name="switch-to-newtonsoftjson"></a><span data-ttu-id="e676a-249">切换到 Newtonsoft.Json</span><span class="sxs-lookup"><span data-stu-id="e676a-249">Switch to Newtonsoft.Json</span></span>

<span data-ttu-id="e676a-250">如果使用的功能`Newtonsoft.Json`中不支持`System.Text.Json`，可以切换回`Newtonsoft.Json`:</span><span class="sxs-lookup"><span data-stu-id="e676a-250">If you're using features of `Newtonsoft.Json` that aren't supported in `System.Text.Json`, you can switch back to `Newtonsoft.Json`:</span></span>

1. <span data-ttu-id="e676a-251">安装[Microsoft.AspNetCore.SignalR.Protocols.NewtonsoftJson](https://www.nuget.org/packages/Microsoft.AspNetCore.SignalR.Protocols.NewtonsoftJson) NuGet 包。</span><span class="sxs-lookup"><span data-stu-id="e676a-251">Install the [Microsoft.AspNetCore.SignalR.Protocols.NewtonsoftJson](https://www.nuget.org/packages/Microsoft.AspNetCore.SignalR.Protocols.NewtonsoftJson) NuGet package.</span></span>
1. <span data-ttu-id="e676a-252">在客户端上链接`AddNewtonsoftJsonProtocol`方法调用`HubConnectionBuilder`实例：</span><span class="sxs-lookup"><span data-stu-id="e676a-252">On the client, chain an `AddNewtonsoftJsonProtocol` method call to the `HubConnectionBuilder` instance:</span></span>

    ```csharp
    new HubConnectionBuilder()
        .WithUrl("/chatHub")
        .AddNewtonsoftJsonProtocol(...)
        .Build();
    ```

1. <span data-ttu-id="e676a-253">在服务器上链接`AddNewtonsoftJsonProtocol`方法调用`AddSignalR`方法调用中`Startup.ConfigureServices`:</span><span class="sxs-lookup"><span data-stu-id="e676a-253">On the server, chain an `AddNewtonsoftJsonProtocol` method call to the `AddSignalR` method call in `Startup.ConfigureServices`:</span></span>

    ```csharp
    services.AddSignalR()
        .AddNewtonsoftJsonProtocol(...);
    ```

## <a name="opt-in-to-runtime-compilation"></a><span data-ttu-id="e676a-254">选择启用运行时编译</span><span class="sxs-lookup"><span data-stu-id="e676a-254">Opt in to runtime compilation</span></span>

<span data-ttu-id="e676a-255">在 3.0 中，运行时编译是选择的方案。</span><span class="sxs-lookup"><span data-stu-id="e676a-255">In 3.0, runtime compilation is an opt-in scenario.</span></span> <span data-ttu-id="e676a-256">若要启用运行时编译，请参阅 https://docs.microsoft.com/aspnet/core/mvc/views/view-compilation?view=aspnetcore-3.0#runtime-compilation 。</span><span class="sxs-lookup"><span data-stu-id="e676a-256">To enable runtime compilation, see https://docs.microsoft.com/aspnet/core/mvc/views/view-compilation?view=aspnetcore-3.0#runtime-compilation.</span></span>
