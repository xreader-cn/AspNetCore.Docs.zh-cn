---
title: ASP.NET Core Blazor 宿主模型
author: guardrex
description: 了解 Blazor WebAssembly 和 Blazor 服务器托管模型。
monikerRange: '>= aspnetcore-3.1'
ms.author: riande
ms.custom: mvc
ms.date: 01/31/2020
no-loc:
- Blazor
- SignalR
uid: blazor/hosting-models
ms.openlocfilehash: 2314ba39e67fbf734807b96de6c54bc94283a67d
ms.sourcegitcommit: d2ba66023884f0dca115ff010bd98d5ed6459283
ms.translationtype: MT
ms.contentlocale: zh-CN
ms.lasthandoff: 02/14/2020
ms.locfileid: "77213309"
---
# <a name="aspnet-core-opno-locblazor-hosting-models"></a><span data-ttu-id="9bea1-103">ASP.NET Core Blazor 宿主模型</span><span class="sxs-lookup"><span data-stu-id="9bea1-103">ASP.NET Core Blazor hosting models</span></span>

<span data-ttu-id="9bea1-104">作者： [Daniel Roth](https://github.com/danroth27)</span><span class="sxs-lookup"><span data-stu-id="9bea1-104">By [Daniel Roth](https://github.com/danroth27)</span></span>

[!INCLUDE[](~/includes/blazorwasm-preview-notice.md)]

Blazor<span data-ttu-id="9bea1-105"> 是一种 web 框架，旨在在基于[WebAssembly](https://webassembly.org/)的 .net 运行时（ *Blazor WebAssembly*）上的浏览器中运行客户端，或者在 ASP.NET Core （ *Blazor 服务器*）中运行服务器端。</span><span class="sxs-lookup"><span data-stu-id="9bea1-105"> is a web framework designed to run client-side in the browser on a [WebAssembly](https://webassembly.org/)-based .NET runtime (*Blazor WebAssembly*) or server-side in ASP.NET Core (*Blazor Server*).</span></span> <span data-ttu-id="9bea1-106">无论采用何种托管模型，应用和组件模型*都是相同*的。</span><span class="sxs-lookup"><span data-stu-id="9bea1-106">Regardless of the hosting model, the app and component models *are the same*.</span></span>

<span data-ttu-id="9bea1-107">若要为本文中所述的托管模型创建项目，请参阅 <xref:blazor/get-started>。</span><span class="sxs-lookup"><span data-stu-id="9bea1-107">To create a project for the hosting models described in this article, see <xref:blazor/get-started>.</span></span>

<span data-ttu-id="9bea1-108">有关高级配置，请参阅 <xref:blazor/hosting-model-configuration>。</span><span class="sxs-lookup"><span data-stu-id="9bea1-108">For advanced configuration, see <xref:blazor/hosting-model-configuration>.</span></span>

## <a name="opno-locblazor-webassembly"></a>Blazor<span data-ttu-id="9bea1-109"> WebAssembly</span><span class="sxs-lookup"><span data-stu-id="9bea1-109"> WebAssembly</span></span>

<span data-ttu-id="9bea1-110">Blazor 的主体宿主模型在 WebAssembly 上的浏览器中运行客户端。</span><span class="sxs-lookup"><span data-stu-id="9bea1-110">The principal hosting model for Blazor is running client-side in the browser on WebAssembly.</span></span> <span data-ttu-id="9bea1-111">将 Blazor 应用、其依赖项以及 .NET 运行时下载到浏览器。</span><span class="sxs-lookup"><span data-stu-id="9bea1-111">The Blazor app, its dependencies, and the .NET runtime are downloaded to the browser.</span></span> <span data-ttu-id="9bea1-112">应用将在浏览器线程中直接执行。</span><span class="sxs-lookup"><span data-stu-id="9bea1-112">The app is executed directly on the browser UI thread.</span></span> <span data-ttu-id="9bea1-113">UI 更新和事件处理发生在同一进程中。</span><span class="sxs-lookup"><span data-stu-id="9bea1-113">UI updates and event handling occur within the same process.</span></span> <span data-ttu-id="9bea1-114">应用的资产将作为静态文件部署到 web 服务器或可为客户端提供静态内容的服务。</span><span class="sxs-lookup"><span data-stu-id="9bea1-114">The app's assets are deployed as static files to a web server or service capable of serving static content to clients.</span></span>

![[!基金.非 LOC （Blazor）]<span data-ttu-id="9bea1-115"> WebAssembly： [！基金.无 LOC （Blazor）] 应用在浏览器内的 UI 线程上运行。</span><span class="sxs-lookup"><span data-stu-id="9bea1-115"> WebAssembly: The Blazor app runs on a UI thread inside the browser.</span></span>](hosting-models/_static/blazor-webassembly.png)

<span data-ttu-id="9bea1-116">若要使用客户端托管模型创建 Blazor 应用，请使用 **Blazor WebAssembly 应用**模板（[dotnet new blazorwasm](/dotnet/core/tools/dotnet-new)）。</span><span class="sxs-lookup"><span data-stu-id="9bea1-116">To create a Blazor app using the client-side hosting model, use the **Blazor WebAssembly App** template ([dotnet new blazorwasm](/dotnet/core/tools/dotnet-new)).</span></span>

<span data-ttu-id="9bea1-117">选择 **Blazor WebAssembly 应用程序**模板之后，你可以选择将应用配置为使用 ASP.NET Core 后端，方法是选中 " **ASP.NET Core 托管**" 复选框（"[dotnet new blazorwasm](/dotnet/core/tools/dotnet-new)"）。</span><span class="sxs-lookup"><span data-stu-id="9bea1-117">After selecting the **Blazor WebAssembly App** template, you have the option of configuring the app to use an ASP.NET Core backend by selecting the **ASP.NET Core hosted** check box ([dotnet new blazorwasm --hosted](/dotnet/core/tools/dotnet-new)).</span></span> <span data-ttu-id="9bea1-118">ASP.NET Core 应用将 Blazor 应用程序提供给客户端。</span><span class="sxs-lookup"><span data-stu-id="9bea1-118">The ASP.NET Core app serves the Blazor app to clients.</span></span> <span data-ttu-id="9bea1-119">Blazor WebAssembly 应用可通过网络使用 web API 调用或[SignalR](xref:signalr/introduction) （<xref:tutorials/signalr-blazor-webassembly>）与服务器交互。</span><span class="sxs-lookup"><span data-stu-id="9bea1-119">The Blazor WebAssembly app can interact with the server over the network using web API calls or [SignalR](xref:signalr/introduction) (<xref:tutorials/signalr-blazor-webassembly>).</span></span>

<span data-ttu-id="9bea1-120">模板包括处理的 `blazor.webassembly.js` 脚本：</span><span class="sxs-lookup"><span data-stu-id="9bea1-120">The templates include the `blazor.webassembly.js` script that handles:</span></span>

* <span data-ttu-id="9bea1-121">下载 .NET 运行时、应用程序和应用程序的依赖项。</span><span class="sxs-lookup"><span data-stu-id="9bea1-121">Downloading the .NET runtime, the app, and the app's dependencies.</span></span>
* <span data-ttu-id="9bea1-122">用于运行应用程序的运行时初始化。</span><span class="sxs-lookup"><span data-stu-id="9bea1-122">Initialization of the runtime to run the app.</span></span>

<span data-ttu-id="9bea1-123">Blazor WebAssembly 宿主模型具有以下几个优点：</span><span class="sxs-lookup"><span data-stu-id="9bea1-123">The Blazor WebAssembly hosting model offers several benefits:</span></span>

* <span data-ttu-id="9bea1-124">没有 .NET 服务器端依赖项。</span><span class="sxs-lookup"><span data-stu-id="9bea1-124">There's no .NET server-side dependency.</span></span> <span data-ttu-id="9bea1-125">应用在下载到客户端之后完全正常运行。</span><span class="sxs-lookup"><span data-stu-id="9bea1-125">The app is fully functioning after downloaded to the client.</span></span>
* <span data-ttu-id="9bea1-126">完全利用客户端资源和功能。</span><span class="sxs-lookup"><span data-stu-id="9bea1-126">Client resources and capabilities are fully leveraged.</span></span>
* <span data-ttu-id="9bea1-127">工作从服务器卸载到客户端。</span><span class="sxs-lookup"><span data-stu-id="9bea1-127">Work is offloaded from the server to the client.</span></span>
* <span data-ttu-id="9bea1-128">不需要 ASP.NET Core web 服务器来托管应用程序。</span><span class="sxs-lookup"><span data-stu-id="9bea1-128">An ASP.NET Core web server isn't required to host the app.</span></span> <span data-ttu-id="9bea1-129">无服务器部署方案可能（例如，通过 CDN 提供应用）。</span><span class="sxs-lookup"><span data-stu-id="9bea1-129">Serverless deployment scenarios are possible (for example, serving the app from a CDN).</span></span>

<span data-ttu-id="9bea1-130">Blazor WebAssembly 托管有一些缺点：</span><span class="sxs-lookup"><span data-stu-id="9bea1-130">There are downsides to Blazor WebAssembly hosting:</span></span>

* <span data-ttu-id="9bea1-131">应用程序限制为浏览器的功能。</span><span class="sxs-lookup"><span data-stu-id="9bea1-131">The app is restricted to the capabilities of the browser.</span></span>
* <span data-ttu-id="9bea1-132">需要支持的客户端硬件和软件（例如，WebAssembly 支持）。</span><span class="sxs-lookup"><span data-stu-id="9bea1-132">Capable client hardware and software (for example, WebAssembly support) is required.</span></span>
* <span data-ttu-id="9bea1-133">下载大小较大，应用需要较长时间才能加载。</span><span class="sxs-lookup"><span data-stu-id="9bea1-133">Download size is larger, and apps take longer to load.</span></span>
* <span data-ttu-id="9bea1-134">.NET 运行时和工具支持不太成熟。</span><span class="sxs-lookup"><span data-stu-id="9bea1-134">.NET runtime and tooling support is less mature.</span></span> <span data-ttu-id="9bea1-135">例如， [.NET Standard](/dotnet/standard/net-standard)支持和调试中存在限制。</span><span class="sxs-lookup"><span data-stu-id="9bea1-135">For example, limitations exist in [.NET Standard](/dotnet/standard/net-standard) support and debugging.</span></span>

## <a name="opno-locblazor-server"></a>Blazor<span data-ttu-id="9bea1-136"> 服务器</span><span class="sxs-lookup"><span data-stu-id="9bea1-136"> Server</span></span>

<span data-ttu-id="9bea1-137">使用 Blazor 服务器托管模型，可在服务器上从 ASP.NET Core 应用中执行应用。</span><span class="sxs-lookup"><span data-stu-id="9bea1-137">With the Blazor Server hosting model, the app is executed on the server from within an ASP.NET Core app.</span></span> <span data-ttu-id="9bea1-138">UI 更新、事件处理和 JavaScript 调用是通过 [SignalR](xref:signalr/introduction) 连接进行处理。</span><span class="sxs-lookup"><span data-stu-id="9bea1-138">UI updates, event handling, and JavaScript calls are handled over a [SignalR](xref:signalr/introduction) connection.</span></span>

![浏览器与应用程序（托管在 ASP.NET Core 应用内的应用程序）通过 [！基金.无 LOC （SignalR）] 连接。](hosting-models/_static/blazor-server.png)

<span data-ttu-id="9bea1-140">若要使用 Blazor 服务器托管模型创建 Blazor 应用，请使用 ASP.NET Core **Blazor 服务器应用**模板（[dotnet new blazorserver](/dotnet/core/tools/dotnet-new)）。</span><span class="sxs-lookup"><span data-stu-id="9bea1-140">To create a Blazor app using the Blazor Server hosting model, use the ASP.NET Core **Blazor Server App** template ([dotnet new blazorserver](/dotnet/core/tools/dotnet-new)).</span></span> <span data-ttu-id="9bea1-141">ASP.NET Core 应用承载 Blazor 服务器应用，并创建客户端连接到 SignalR 终结点。</span><span class="sxs-lookup"><span data-stu-id="9bea1-141">The ASP.NET Core app hosts the Blazor Server app and creates the SignalR endpoint where clients connect.</span></span>

<span data-ttu-id="9bea1-142">ASP.NET Core 应用引用要添加的应用 `Startup` 类：</span><span class="sxs-lookup"><span data-stu-id="9bea1-142">The ASP.NET Core app references the app's `Startup` class to add:</span></span>

* <span data-ttu-id="9bea1-143">服务器端服务。</span><span class="sxs-lookup"><span data-stu-id="9bea1-143">Server-side services.</span></span>
* <span data-ttu-id="9bea1-144">请求处理管道的应用。</span><span class="sxs-lookup"><span data-stu-id="9bea1-144">The app to the request handling pipeline.</span></span>

<span data-ttu-id="9bea1-145">`blazor.server.js` 脚本&dagger; 建立客户端连接。</span><span class="sxs-lookup"><span data-stu-id="9bea1-145">The `blazor.server.js` script&dagger; establishes the client connection.</span></span> <span data-ttu-id="9bea1-146">应用负责根据需要保存和还原应用状态（例如，在网络连接丢失的情况下）。</span><span class="sxs-lookup"><span data-stu-id="9bea1-146">It's the app's responsibility to persist and restore app state as required (for example, in the event of a lost network connection).</span></span>

<span data-ttu-id="9bea1-147">Blazor Server 宿主模型具有以下几个优点：</span><span class="sxs-lookup"><span data-stu-id="9bea1-147">The Blazor Server hosting model offers several benefits:</span></span>

* <span data-ttu-id="9bea1-148">下载大小明显小于 Blazor WebAssembly 应用，且应用加载速度快得多。</span><span class="sxs-lookup"><span data-stu-id="9bea1-148">Download size is significantly smaller than a Blazor WebAssembly app, and the app loads much faster.</span></span>
* <span data-ttu-id="9bea1-149">应用充分利用服务器功能，包括使用任何与 .NET Core 兼容的 Api。</span><span class="sxs-lookup"><span data-stu-id="9bea1-149">The app takes full advantage of server capabilities, including use of any .NET Core compatible APIs.</span></span>
* <span data-ttu-id="9bea1-150">服务器上的 .NET Core 用于运行应用程序，因此现有的 .NET 工具（如调试）可按预期方式工作。</span><span class="sxs-lookup"><span data-stu-id="9bea1-150">.NET Core on the server is used to run the app, so existing .NET tooling, such as debugging, works as expected.</span></span>
* <span data-ttu-id="9bea1-151">支持瘦客户端。</span><span class="sxs-lookup"><span data-stu-id="9bea1-151">Thin clients are supported.</span></span> <span data-ttu-id="9bea1-152">例如，Blazor Server apps 适用于不支持 WebAssembly 的浏览器以及资源受限设备上的浏览器。</span><span class="sxs-lookup"><span data-stu-id="9bea1-152">For example, Blazor Server apps work with browsers that don't support WebAssembly and on resource-constrained devices.</span></span>
* <span data-ttu-id="9bea1-153">应用程序的 .NET/C#代码库（包括应用程序的组件代码）不会提供给客户端。</span><span class="sxs-lookup"><span data-stu-id="9bea1-153">The app's .NET/C# code base, including the app's component code, isn't served to clients.</span></span>

<span data-ttu-id="9bea1-154">Blazor Server 宿主有一些缺点：</span><span class="sxs-lookup"><span data-stu-id="9bea1-154">There are downsides to Blazor Server hosting:</span></span>

* <span data-ttu-id="9bea1-155">通常存在较高的延迟。</span><span class="sxs-lookup"><span data-stu-id="9bea1-155">Higher latency usually exists.</span></span> <span data-ttu-id="9bea1-156">每个用户交互都涉及网络跃点。</span><span class="sxs-lookup"><span data-stu-id="9bea1-156">Every user interaction involves a network hop.</span></span>
* <span data-ttu-id="9bea1-157">无脱机支持。</span><span class="sxs-lookup"><span data-stu-id="9bea1-157">There's no offline support.</span></span> <span data-ttu-id="9bea1-158">如果客户端连接失败，应用将停止工作。</span><span class="sxs-lookup"><span data-stu-id="9bea1-158">If the client connection fails, the app stops working.</span></span>
* <span data-ttu-id="9bea1-159">对于包含多个用户的应用而言，可伸缩性非常困难。</span><span class="sxs-lookup"><span data-stu-id="9bea1-159">Scalability is challenging for apps with many users.</span></span> <span data-ttu-id="9bea1-160">服务器必须管理多个客户端连接并处理客户端状态。</span><span class="sxs-lookup"><span data-stu-id="9bea1-160">The server must manage multiple client connections and handle client state.</span></span>
* <span data-ttu-id="9bea1-161">为应用提供服务需要 ASP.NET Core 服务器。</span><span class="sxs-lookup"><span data-stu-id="9bea1-161">An ASP.NET Core server is required to serve the app.</span></span> <span data-ttu-id="9bea1-162">不可能的无服务器部署方案（例如，通过 CDN 为应用提供服务）。</span><span class="sxs-lookup"><span data-stu-id="9bea1-162">Serverless deployment scenarios aren't possible (for example, serving the app from a CDN).</span></span>

<span data-ttu-id="9bea1-163">&dagger;`blazor.server.js` 脚本是从 ASP.NET Core 共享框架中的嵌入资源提供的。</span><span class="sxs-lookup"><span data-stu-id="9bea1-163">&dagger;The `blazor.server.js` script is served from an embedded resource in the ASP.NET Core shared framework.</span></span>

### <a name="comparison-to-server-rendered-ui"></a><span data-ttu-id="9bea1-164">与服务器呈现的 UI 的比较</span><span class="sxs-lookup"><span data-stu-id="9bea1-164">Comparison to server-rendered UI</span></span>

<span data-ttu-id="9bea1-165">了解 Blazor Server 应用程序的一种方法是，了解它与传统模型的不同之处在于，使用 Razor 视图或 Razor Pages 在 ASP.NET Core 应用程序中呈现 UI。</span><span class="sxs-lookup"><span data-stu-id="9bea1-165">One way to understand Blazor Server apps is to understand how it differs from traditional models for rendering UI in ASP.NET Core apps using Razor views or Razor Pages.</span></span> <span data-ttu-id="9bea1-166">这两种模型都使用 Razor 语言来描述 HTML 内容，但在显示标记的方式上差别很大。</span><span class="sxs-lookup"><span data-stu-id="9bea1-166">Both models use the Razor language to describe HTML content, but they significantly differ in how markup is rendered.</span></span>

<span data-ttu-id="9bea1-167">在显示 Razor 页面或视图时，每行 Razor 代码都会以文本形式发出 HTML。</span><span class="sxs-lookup"><span data-stu-id="9bea1-167">When a Razor Page or view is rendered, every line of Razor code emits HTML in text form.</span></span> <span data-ttu-id="9bea1-168">在呈现后，服务器将释放页面或视图实例，包括生成的任何状态。</span><span class="sxs-lookup"><span data-stu-id="9bea1-168">After rendering, the server disposes of the page or view instance, including any state that was produced.</span></span> <span data-ttu-id="9bea1-169">当对该页的另一请求出现时，例如，当服务器验证失败并显示验证摘要时：</span><span class="sxs-lookup"><span data-stu-id="9bea1-169">When another request for the page occurs, for instance when server validation fails and the validation summary is displayed:</span></span>

* <span data-ttu-id="9bea1-170">整个页面将再次重新呈现到 HTML 文本。</span><span class="sxs-lookup"><span data-stu-id="9bea1-170">The entire page is rerendered to HTML text again.</span></span>
* <span data-ttu-id="9bea1-171">该页将发送到客户端。</span><span class="sxs-lookup"><span data-stu-id="9bea1-171">The page is sent to the client.</span></span>

<span data-ttu-id="9bea1-172">Blazor 应用由 UI 的可重用元素组成，这些元素称为*组件*。</span><span class="sxs-lookup"><span data-stu-id="9bea1-172">A Blazor app is composed of reusable elements of UI called *components*.</span></span> <span data-ttu-id="9bea1-173">组件包含C#代码、标记和其他组件。</span><span class="sxs-lookup"><span data-stu-id="9bea1-173">A component contains C# code, markup, and other components.</span></span> <span data-ttu-id="9bea1-174">呈现组件时，Blazor 生成包含的组件的图，类似于 HTML 或 XML 文档对象模型（DOM）。</span><span class="sxs-lookup"><span data-stu-id="9bea1-174">When a component is rendered, Blazor produces a graph of the included components similar to an HTML or XML Document Object Model (DOM).</span></span> <span data-ttu-id="9bea1-175">此图包含属性和字段中保存的组件状态。</span><span class="sxs-lookup"><span data-stu-id="9bea1-175">This graph includes component state held in properties and fields.</span></span> Blazor<span data-ttu-id="9bea1-176"> 计算组件图以生成标记的二进制表示形式。</span><span class="sxs-lookup"><span data-stu-id="9bea1-176"> evaluates the component graph to produce a binary representation of the markup.</span></span> <span data-ttu-id="9bea1-177">二进制格式可以是：</span><span class="sxs-lookup"><span data-stu-id="9bea1-177">The binary format can be:</span></span>

* <span data-ttu-id="9bea1-178">转换为 HTML 文本（在预呈现&dagger;期间）。</span><span class="sxs-lookup"><span data-stu-id="9bea1-178">Turned into HTML text (during prerendering&dagger;).</span></span>
* <span data-ttu-id="9bea1-179">用于在常规呈现期间有效地更新标记。</span><span class="sxs-lookup"><span data-stu-id="9bea1-179">Used to efficiently update the markup during regular rendering.</span></span>

<span data-ttu-id="9bea1-180">&dagger;预*呈现*&ndash; 请求的 Razor 组件在服务器上编译为静态 HTML 并发送到客户端，并在其中呈现给用户。</span><span class="sxs-lookup"><span data-stu-id="9bea1-180">&dagger;*Prerendering* &ndash; The requested Razor component is compiled on the server into static HTML and sent to the client, where it's rendered to the user.</span></span> <span data-ttu-id="9bea1-181">在客户端与服务器之间建立连接后，会将组件的静态预呈现元素替换为交互式元素。</span><span class="sxs-lookup"><span data-stu-id="9bea1-181">After the connection is made between the client and the server, the component's static prerendered elements are replaced with interactive elements.</span></span> <span data-ttu-id="9bea1-182">预呈现使应用对用户的响应速度更快。</span><span class="sxs-lookup"><span data-stu-id="9bea1-182">Prerendering makes the app feel more responsive to the user.</span></span>

<span data-ttu-id="9bea1-183">Blazor 中的 UI 更新由以下用户触发：</span><span class="sxs-lookup"><span data-stu-id="9bea1-183">A UI update in Blazor is triggered by:</span></span>

* <span data-ttu-id="9bea1-184">用户交互，如选择一个按钮。</span><span class="sxs-lookup"><span data-stu-id="9bea1-184">User interaction, such as selecting a button.</span></span>
* <span data-ttu-id="9bea1-185">应用触发器，如计时器。</span><span class="sxs-lookup"><span data-stu-id="9bea1-185">App triggers, such as a timer.</span></span>

<span data-ttu-id="9bea1-186">关系图为重新呈现，并计算了 UI*差异*（差异）。</span><span class="sxs-lookup"><span data-stu-id="9bea1-186">The graph is rerendered, and a UI *diff* (difference) is calculated.</span></span> <span data-ttu-id="9bea1-187">这种差异是更新客户端上 UI 所需的最小 DOM 编辑集。</span><span class="sxs-lookup"><span data-stu-id="9bea1-187">This diff is the smallest set of DOM edits required to update the UI on the client.</span></span> <span data-ttu-id="9bea1-188">将以二进制格式将差异发送到客户端，并由浏览器应用。</span><span class="sxs-lookup"><span data-stu-id="9bea1-188">The diff is sent to the client in a binary format and applied by the browser.</span></span>

<span data-ttu-id="9bea1-189">当用户在客户端上导航掉组件后，将释放该组件。</span><span class="sxs-lookup"><span data-stu-id="9bea1-189">A component is disposed after the user navigates away from it on the client.</span></span> <span data-ttu-id="9bea1-190">当用户与组件交互时，组件的状态（服务、资源）必须保存在服务器的内存中。</span><span class="sxs-lookup"><span data-stu-id="9bea1-190">While a user is interacting with a component, the component's state (services, resources) must be held in the server's memory.</span></span> <span data-ttu-id="9bea1-191">由于多个组件的状态可能同时由服务器维护，因此内存耗尽是必须解决的问题。</span><span class="sxs-lookup"><span data-stu-id="9bea1-191">Because the state of many components might be maintained by the server concurrently, memory exhaustion is a concern that must be addressed.</span></span> <span data-ttu-id="9bea1-192">有关如何创作 Blazor Server 应用程序以确保最大程度地使用服务器内存的指导，请参阅 <xref:security/blazor/server>。</span><span class="sxs-lookup"><span data-stu-id="9bea1-192">For guidance on how to author a Blazor Server app to ensure the best use of server memory, see <xref:security/blazor/server>.</span></span>

### <a name="circuits"></a><span data-ttu-id="9bea1-193">而言</span><span class="sxs-lookup"><span data-stu-id="9bea1-193">Circuits</span></span>

<span data-ttu-id="9bea1-194">Blazor Server 应用是在[ASP.NET Core SignalR](xref:signalr/introduction)的基础上构建的。</span><span class="sxs-lookup"><span data-stu-id="9bea1-194">A Blazor Server app is built on top of [ASP.NET Core SignalR](xref:signalr/introduction).</span></span> <span data-ttu-id="9bea1-195">每个客户端通过一个或多个称为*线路*的 SignalR 连接与服务器通信。</span><span class="sxs-lookup"><span data-stu-id="9bea1-195">Each client communicates to the server over one or more SignalR connections called a *circuit*.</span></span> <span data-ttu-id="9bea1-196">线路是对可容忍暂时网络中断的 SignalR 连接 Blazor抽象。</span><span class="sxs-lookup"><span data-stu-id="9bea1-196">A circuit is Blazor's abstraction over SignalR connections that can tolerate temporary network interruptions.</span></span> <span data-ttu-id="9bea1-197">当 Blazor 客户端发现 SignalR 连接已断开连接时，它会尝试使用新的 SignalR 连接重新连接到服务器。</span><span class="sxs-lookup"><span data-stu-id="9bea1-197">When a Blazor client sees that the SignalR connection is disconnected, it attempts to reconnect to the server using a new SignalR connection.</span></span>

<span data-ttu-id="9bea1-198">连接到 Blazor Server 应用程序的每个浏览器屏幕（浏览器选项卡或 iframe）都使用 SignalR 连接。</span><span class="sxs-lookup"><span data-stu-id="9bea1-198">Each browser screen (browser tab or iframe) that is connected to a Blazor Server app uses a SignalR connection.</span></span> <span data-ttu-id="9bea1-199">与典型服务器呈现的应用相比，这一点还有一个重要的区别。</span><span class="sxs-lookup"><span data-stu-id="9bea1-199">This is yet another important distinction compared to typical server-rendered apps.</span></span> <span data-ttu-id="9bea1-200">在服务器呈现的应用程序中，在多个浏览器屏幕中打开同一应用程序通常不会转换为服务器上的其他资源需求。</span><span class="sxs-lookup"><span data-stu-id="9bea1-200">In a server-rendered app, opening the same app in multiple browser screens typically doesn't translate into additional resource demands on the server.</span></span> <span data-ttu-id="9bea1-201">在 Blazor Server 应用程序中，每个浏览器屏幕都需要单独的线路和组件状态的单独实例，由服务器来管理。</span><span class="sxs-lookup"><span data-stu-id="9bea1-201">In a Blazor Server app, each browser screen requires a separate circuit and separate instances of component state to be managed by the server.</span></span>

Blazor<span data-ttu-id="9bea1-202"> 考虑关闭浏览器选项卡或导航到外部 URL，*正常*终止。</span><span class="sxs-lookup"><span data-stu-id="9bea1-202"> considers closing a browser tab or navigating to an external URL a *graceful* termination.</span></span> <span data-ttu-id="9bea1-203">如果正常终止，则会立即释放线路和关联的资源。</span><span class="sxs-lookup"><span data-stu-id="9bea1-203">In the event of a graceful termination, the circuit and associated resources are immediately released.</span></span> <span data-ttu-id="9bea1-204">例如，由于网络中断，客户端也可能断开连接。</span><span class="sxs-lookup"><span data-stu-id="9bea1-204">A client may also disconnect non-gracefully, for instance due to a network interruption.</span></span> Blazor<span data-ttu-id="9bea1-205"> 服务器会将断开连接的线路存储为可配置的间隔，以允许客户端重新连接。</span><span class="sxs-lookup"><span data-stu-id="9bea1-205"> Server stores disconnected circuits for a configurable interval to allow the client to reconnect.</span></span> <span data-ttu-id="9bea1-206">有关详细信息，请参阅重新[连接到同一服务器](xref:blazor/hosting-model-configuration#reconnection-to-the-same-server)。</span><span class="sxs-lookup"><span data-stu-id="9bea1-206">For more information, see [Reconnection to the same server](xref:blazor/hosting-model-configuration#reconnection-to-the-same-server).</span></span>

### <a name="ui-latency"></a><span data-ttu-id="9bea1-207">UI 延迟</span><span class="sxs-lookup"><span data-stu-id="9bea1-207">UI Latency</span></span>

<span data-ttu-id="9bea1-208">UI 延迟是指从启动的操作到 UI 更新的时间。</span><span class="sxs-lookup"><span data-stu-id="9bea1-208">UI latency is the time it takes from an initiated action to the time the UI is updated.</span></span> <span data-ttu-id="9bea1-209">对于应用程序来说，更小的 UI 延迟值非常适合应用程序对用户的响应。</span><span class="sxs-lookup"><span data-stu-id="9bea1-209">Smaller values for UI latency are imperative for an app to feel responsive to a user.</span></span> <span data-ttu-id="9bea1-210">在 Blazor 服务器应用中，每个操作都将发送到服务器并进行处理，并将向后发送 UI 差异。</span><span class="sxs-lookup"><span data-stu-id="9bea1-210">In a Blazor Server app, each action is sent to the server, processed, and a UI diff is sent back.</span></span> <span data-ttu-id="9bea1-211">因此，UI 延迟是网络延迟和处理操作时服务器延迟的总和。</span><span class="sxs-lookup"><span data-stu-id="9bea1-211">Consequently, UI latency is the sum of network latency and the server latency in processing the action.</span></span>

<span data-ttu-id="9bea1-212">对于仅限于专用公司网络的业务线应用，对用户而言，由于网络延迟导致的延迟通常是让的。</span><span class="sxs-lookup"><span data-stu-id="9bea1-212">For a line of business app that's limited to a private corporate network, the effect on user perceptions of latency due to network latency are usually imperceptible.</span></span> <span data-ttu-id="9bea1-213">对于通过 Internet 部署的应用，用户可能会对延迟造成明显的影响，尤其是用户广泛分散于各地。</span><span class="sxs-lookup"><span data-stu-id="9bea1-213">For an app deployed over the Internet, latency may become noticeable to users, particularly if users are widely distributed geographically.</span></span>

<span data-ttu-id="9bea1-214">内存使用率还会导致应用延迟。</span><span class="sxs-lookup"><span data-stu-id="9bea1-214">Memory usage can also contribute to app latency.</span></span> <span data-ttu-id="9bea1-215">增加的内存使用会导致频繁垃圾收集或将内存分页到磁盘，这两者都会降低应用程序性能，进而增加 UI 延迟。</span><span class="sxs-lookup"><span data-stu-id="9bea1-215">Increased memory usage results in frequent garbage collection or paging memory to disk, both of which degrade app performance and consequently increase UI latency.</span></span> <span data-ttu-id="9bea1-216">有关详细信息，请参阅 <xref:security/blazor/server>。</span><span class="sxs-lookup"><span data-stu-id="9bea1-216">For more information, see <xref:security/blazor/server>.</span></span>

Blazor<span data-ttu-id="9bea1-217"> Server apps 应进行优化，以通过减少网络延迟和内存使用率来最大限度地减少 UI 延迟。</span><span class="sxs-lookup"><span data-stu-id="9bea1-217"> Server apps should be optimized to minimize UI latency by reducing network latency and memory usage.</span></span> <span data-ttu-id="9bea1-218">有关测量网络延迟的方法，请参阅 <xref:host-and-deploy/blazor/server#measure-network-latency>。</span><span class="sxs-lookup"><span data-stu-id="9bea1-218">For an approach to measuring network latency, see <xref:host-and-deploy/blazor/server#measure-network-latency>.</span></span> <span data-ttu-id="9bea1-219">有关 SignalR 和 Blazor的详细信息，请参阅：</span><span class="sxs-lookup"><span data-stu-id="9bea1-219">For more information on SignalR and Blazor, see:</span></span>

* <xref:host-and-deploy/blazor/server>
* <xref:security/blazor/server>

### <a name="connection-to-the-server"></a><span data-ttu-id="9bea1-220">与服务器的连接</span><span class="sxs-lookup"><span data-stu-id="9bea1-220">Connection to the server</span></span>

Blazor<span data-ttu-id="9bea1-221"> Server 应用需要与服务器建立活动的 SignalR 连接。</span><span class="sxs-lookup"><span data-stu-id="9bea1-221"> Server apps require an active SignalR connection to the server.</span></span> <span data-ttu-id="9bea1-222">如果连接丢失，应用会尝试重新连接到服务器。</span><span class="sxs-lookup"><span data-stu-id="9bea1-222">If the connection is lost, the app attempts to reconnect to the server.</span></span> <span data-ttu-id="9bea1-223">只要客户端的状态仍在内存中，客户端会话便会恢复而不会失去状态。</span><span class="sxs-lookup"><span data-stu-id="9bea1-223">As long as the client's state is still in memory, the client session resumes without losing state.</span></span>

<span data-ttu-id="9bea1-224">Blazor Server 应用 prerenders 为响应第一个客户端请求，该请求在服务器上设置 UI 状态。</span><span class="sxs-lookup"><span data-stu-id="9bea1-224">A Blazor Server app prerenders in response to the first client request, which sets up the UI state on the server.</span></span> <span data-ttu-id="9bea1-225">当客户端尝试创建 SignalR 连接时，客户端必须重新连接到同一服务器。</span><span class="sxs-lookup"><span data-stu-id="9bea1-225">When the client attempts to create a SignalR connection, the client must reconnect to the same server.</span></span> <span data-ttu-id="9bea1-226">使用多台后端服务器 Blazor 服务器应用应为 SignalR 连接实现*粘滞会话*。</span><span class="sxs-lookup"><span data-stu-id="9bea1-226">Blazor Server apps that use more than one backend server should implement *sticky sessions* for SignalR connections.</span></span>

<span data-ttu-id="9bea1-227">我们建议将 [Azure SignalR 服务](/azure/azure-signalr)用于 Blazor Server 应用。</span><span class="sxs-lookup"><span data-stu-id="9bea1-227">We recommend using the [Azure SignalR Service](/azure/azure-signalr) for Blazor Server apps.</span></span> <span data-ttu-id="9bea1-228">该服务允许将 Blazor Server 应用扩展到大量并发 SignalR 连接。</span><span class="sxs-lookup"><span data-stu-id="9bea1-228">The service allows for scaling up a Blazor Server app to a large number of concurrent SignalR connections.</span></span> <span data-ttu-id="9bea1-229">可以通过将服务的 `ServerStickyMode` 选项或配置值设置为 `Required`，为 Azure SignalR 服务启用粘滞会话。</span><span class="sxs-lookup"><span data-stu-id="9bea1-229">Sticky sessions are enabled for the Azure SignalR Service by setting the service's `ServerStickyMode` option or configuration value to `Required`.</span></span> <span data-ttu-id="9bea1-230">有关详细信息，请参阅 <xref:host-and-deploy/blazor/server#signalr-configuration>。</span><span class="sxs-lookup"><span data-stu-id="9bea1-230">For more information, see <xref:host-and-deploy/blazor/server#signalr-configuration>.</span></span>

<span data-ttu-id="9bea1-231">使用 IIS 时，粘滞会话通过应用程序请求路由启用。</span><span class="sxs-lookup"><span data-stu-id="9bea1-231">When using IIS, sticky sessions are enabled with Application Request Routing.</span></span> <span data-ttu-id="9bea1-232">有关详细信息，请参阅[使用应用程序请求路由实现 HTTP 负载均衡](/iis/extensions/configuring-application-request-routing-arr/http-load-balancing-using-application-request-routing)。</span><span class="sxs-lookup"><span data-stu-id="9bea1-232">For more information, see [HTTP Load Balancing using Application Request Routing](/iis/extensions/configuring-application-request-routing-arr/http-load-balancing-using-application-request-routing).</span></span>

## <a name="additional-resources"></a><span data-ttu-id="9bea1-233">其他资源</span><span class="sxs-lookup"><span data-stu-id="9bea1-233">Additional resources</span></span>

* <xref:blazor/get-started>
* <xref:signalr/introduction>
* <xref:blazor/hosting-model-configuration>
* <xref:tutorials/signalr-blazor-webassembly>
